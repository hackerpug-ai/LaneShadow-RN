# Pre-existing Issues for US-030

Confirmed via `git stash` test that both failures exist on the base commit (b2ec3f3) before any US-030 changes.

## Typecheck
- `components/map/route-polyline.test.tsx:109` — parse errors (pre-existing, unrelated file)

## Lint
- ESLint config issue: "react-native" plugin not found in configuration object (pre-existing config issue)

## These do NOT affect US-030 changes.
