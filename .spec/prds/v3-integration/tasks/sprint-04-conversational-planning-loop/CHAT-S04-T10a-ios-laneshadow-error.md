# CHAT-S04-T10a iOS Review Artifact

## Acceptance Criteria
- [x] AC-1 Swift `LaneShadowError` typed taxonomy mirrors RN/server canonical codes, including `SESSION_NOT_FOUND`, `RATE_LIMIT_EXCEEDED`, `PLAN_LIMIT_EXCEEDED`, `AGENT_TIMEOUT`, `UNAUTHENTICATED`, `NETWORK_TIMEOUT`, `AUTH_REQUIRED`, `NOT_FOUND`, `INVALID_INPUT`, `AGENT_BUDGET_EXCEEDED`, and `AGENT_LOOP_DETECTED` if present in `server/convex/errors.ts`. ← PASS: all 28 server/lib/errors.ts + server/convex/errors.ts codes present in LaneShadowError.swift and codeMap in LaneShadowErrorMapping.swift (evidence: LaneShadowError.swift:4–38, LaneShadowErrorMapping.swift:54–83; round-2 review 2026-05-02)
- [x] AC-2 Unknown server messages/coded failures fall back to `.unknown(original message)` without leaking raw code as user copy. ← PASS: mapConvexPayload returns `.unknown(payload)` for unrecognised JSON codes (LaneShadowErrorMapping.swift:28–33); mapStructuredMessage returns `.unknown(message)` for unrecognised tokens (LaneShadowErrorMapping.swift:46–50); bodyText for .unknown always returns the generic "Something went wrong…" copy (LaneShadowError.swift:104–105). Both test assertions fixed in round 3: LaneShadowErrorTests.swift:86,94 now assert generic copy and rawMessage preservation (round-3 review 2026-05-02).
- [x] AC-3 `UNAUTHENTICATED` triggers sign-out cleanup and routes back to sign-in.
- [x] AC-4 ErrorScreen Try again redispatches cached `sendMessage`/planning retry payload through production wiring. ← PASS (round 3): `ErrorScreenViewModel` now receives `any LaneShadowPlanningDataProviding` (ErrorScreenViewModel.swift:22,28); `handleTryAgain()` calls `convexClient.createPlanningSession(firstMessage:)` then `convexClient.sendPlanningMessage(sessionId:content:currentLocation:)` in a `@MainActor Task`, dispatches `.sendMessageWithSession` with real backend session ID (ErrorScreenViewModel.swift:116–137). `AppFlowView` passes `appEnvironment.convexClient` in both error screen callsites (AppFlowView.swift:32,89). `ErrorScreenWiringTests` now holds a `StubLaneShadowConvexClient` and asserts `createPlanningSessionCalls` and `sendPlanningMessageCalls` (ErrorScreenWiringTests.swift:33–40). Production implementation confirmed real; test gate blocked by TC-1 test bug — see TC-1 annotation.
- [x] AC-5 ErrorScreen Start over dispatches new-session/idle reset and clears app route/state.
- [x] AC-6 `PLAN_LIMIT_EXCEEDED` suppresses retry and shows canonical no-retry plan-limit copy.

## Test Criteria
- [ ] TC-1 Focused `xcodebuild test` for the touched error/planning/root suites passes. ← FAIL (round 3): 12/13 tests pass. One failure: `planningScreenFreeTextSendFailureCachesPayloadAndRetryReplaysIt` at PlanningScreenWiringTests.swift:287 — `Expectation failed: (context.chatStore.flowState.phase → .error) == .planning`. Root cause: `context.client.stubSendPlanningMessageError = LaneShadowError.agentTimeout` set at line 258 is not cleared before `retryViewModel.handleTryAgain()` at line 279. `handleTryAgain()` awaits `sendPlanningMessage` in the same `@MainActor Task`; since the stub error is still active, `sendPlanningMessage` throws `agentTimeout`, the catch block dispatches `.planningError`, and the flow reverts to `.error`. Fix: add `context.client.stubSendPlanningMessageError = nil` between lines 271 and 273 (after asserting `.error`, before creating `retryViewModel`). This correctly models "initial send fails, retry succeeds."
- [x] TC-2 `xcodebuild build -project ios/LaneShadow.xcodeproj -scheme LaneShadow -destination 'platform=iOS Simulator,name=iPhone 16' BUILD SUCCEEDED` passes. ← PASS (round 3): BUILD SUCCEEDED confirmed.
- [ ] TC-3 `swiftformat --lint` on changed files passes cleanly. ← FAIL (round 4): 3/16 branch-local Swift files still fail swiftformat --lint. Round-3 implementer only formatted the 6 files in their fix commit, leaving LaneShadowErrorRouter.swift:19 (consecutiveBlankLines), LaneShadowErrorMapping.swift:82,101 (trailingCommas), ErrorScreen.swift:200 (consecutiveBlankLines). Fix: run `swiftformat ios/LaneShadow/Services/LaneShadowErrorRouter.swift ios/LaneShadow/Services/LaneShadowErrorMapping.swift ios/LaneShadow/Views/Templates/ErrorScreen.swift`.
- [ ] TC-4 Changed-file SwiftLint stays free of serious branch-local issues. ← PARTIAL (round 3, same as round 2): no `error:` severity violations; warning-level issues remain (`type_body_length` LaneShadowError.swift, `function_body_length` LaneShadowErrorTests.swift and PlanningScreenWiringTests.swift). Not blocking independently but persists.
- [x] TC-5 No `StorySnapshotTests` or `LSIconTypeSafetyTests` churn remains in the branch diff.
