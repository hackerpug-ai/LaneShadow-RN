# CHAT-S04-T10a iOS Review Artifact

## Acceptance Criteria
- [x] AC-1 Swift `LaneShadowError` typed taxonomy mirrors RN/server canonical codes, including `SESSION_NOT_FOUND`, `RATE_LIMIT_EXCEEDED`, `PLAN_LIMIT_EXCEEDED`, `AGENT_TIMEOUT`, `UNAUTHENTICATED`, `NETWORK_TIMEOUT`, `AUTH_REQUIRED`, `NOT_FOUND`, `INVALID_INPUT`, `AGENT_BUDGET_EXCEEDED`, and `AGENT_LOOP_DETECTED` if present in `server/convex/errors.ts`. ← PASS: all 28 server/lib/errors.ts + server/convex/errors.ts codes present in LaneShadowError.swift and codeMap in LaneShadowErrorMapping.swift (evidence: LaneShadowError.swift:4–38, LaneShadowErrorMapping.swift:54–83; round-2 review 2026-05-02)
- [x] AC-2 Unknown server messages/coded failures fall back to `.unknown(original message)` without leaking raw code as user copy. ← PASS: mapConvexPayload returns `.unknown(payload)` for unrecognised JSON codes (LaneShadowErrorMapping.swift:28–33); mapStructuredMessage returns `.unknown(message)` for unrecognised tokens (LaneShadowErrorMapping.swift:46–50); bodyText for .unknown always returns the generic "Something went wrong…" copy (LaneShadowError.swift:104–105). Two test assertions in LaneShadowErrorTests.swift:86,94 incorrectly expect `errorDescription` to expose the original token — those assertions need correction under TC-1 but the AC itself is satisfied by the implementation.
- [x] AC-3 `UNAUTHENTICATED` triggers sign-out cleanup and routes back to sign-in.
- [ ] AC-4 ErrorScreen Try again redispatches cached `sendMessage`/planning retry payload through production wiring. ← FAIL (HIGH semantic stub — persists round 2): `handleTryAgain()` at ErrorScreenViewModel.swift:104 dispatches `chatStore.dispatch(.sendMessage(retryPayload))`. `reduceError(.sendMessage)` at RideFlow.swift:661 only transitions local state to `.planning` with a synthetic session ID — no `createPlanningSession` or `sendPlanningMessage` Convex call is made. The resulting PlanningScreen loads with a non-existent backend session and receives no messages. Test Theatre: `errorScreen_tryAgain_redispatchesSendMessage` asserts only on `chatStore.flowState.phase`, `sessionId`, and `sessionStore.activeSessionId` (all local state), never verifying a Convex API call (ErrorScreenWiringTests.swift:29–34). Fix: `ErrorScreenViewModel` must receive a `convexClient` (or `LaneShadowPlanningDataProviding`), and `handleTryAgain()` must call `createPlanningSession(firstMessage:)` followed by `sendPlanningMessage(sessionId:content:currentLocation:)`, then dispatch `.sendMessageWithSession` with the real backend session ID. Test must add a mock client that captures `sendPlanningMessage` invocations.
- [x] AC-5 ErrorScreen Start over dispatches new-session/idle reset and clears app route/state.
- [x] AC-6 `PLAN_LIMIT_EXCEEDED` suppresses retry and shows canonical no-retry plan-limit copy.

## Test Criteria
- [ ] TC-1 Focused `xcodebuild test` for the touched error/planning/root suites passes. ← FAIL (round 2): xcodebuild reports `** TEST FAILED **`; `LaneShadow Error Tests` suite fails with 2 issues: (1) `test_laneShadowError_map_unknownMessage_fallsBack` at LaneShadowErrorTests.swift:86 — asserts `mapped.errorDescription == "some uncoded message"` but implementation correctly returns generic copy; (2) `test_laneShadowError_map_unknownCodedMessage_preservesOriginalMessage` at LaneShadowErrorTests.swift:94 — asserts `mapped.errorDescription == "STREAM_DOWN"` which would leak a raw code as user copy in violation of AC-2. Fix: change both assertions to `#expect(mapped.errorDescription == "Something went wrong. Please try again.")` and keep/add `#expect(mapped.rawMessage == <original>)` to verify internal preservation.
- [x] TC-2 `xcodebuild build -project ios/LaneShadow.xcodeproj -scheme LaneShadow -destination 'platform=iOS Simulator,name=iPhone 16' -quiet ONLY_ACTIVE_ARCH=YES build` passes.
- [ ] TC-3 `swiftformat --lint ios/` passes cleanly. ← FAIL (round 2): swiftformat --lint exits 1 with 5 errors in 4 branch-local files: LaneShadowErrorRouter.swift:19 (consecutiveBlankLines), ErrorScreenContainer.swift:25 (consecutiveBlankLines), LaneShadowErrorMapping.swift:82,101 (trailingCommas), ErrorScreen.swift:200 (consecutiveBlankLines). Fix: run `swiftformat` on those 4 files.
- [ ] TC-4 Changed-file SwiftLint stays free of serious branch-local issues. ← PARTIAL (round 2): no `error:` severity violations; warning-level issues remain in branch-local files including `type_body_length` in LaneShadowError.swift:6 (264 lines > 250), `function_body_length` in LaneShadowErrorTests.swift:10 and PlanningScreenWiringTests.swift:13,82, and `trailing_newline` in LaneShadowErrorRouter.swift:19, ErrorScreenContainer.swift:25, ErrorScreen.swift:200. Not blocking on their own but trailing_newline warnings in new files should be cleaned up alongside TC-3 swiftformat fixes.
- [x] TC-5 No `StorySnapshotTests` or `LSIconTypeSafetyTests` churn remains in the branch diff.
