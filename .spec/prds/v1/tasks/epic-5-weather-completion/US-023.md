# US-023: Wire rain+temp overlays into planRideOrchestrator

> Task ID: US-023
> Type: FEATURE
> Priority: P0
> Estimate: 30 minutes
> Assignee: backend-engineer

## CRITICAL CONSTRAINTS

### MUST
- Update orchestrator to store rain and temperature overlays alongside wind on routeSnapshot
- Pass all overlay data through to buildOptionsFromResults
- Ensure partial weather failure does not block the pipeline

### NEVER
- Drop wind overlay data during the refactor
- Allow weather failure to prevent route generation

### STRICTLY
- routeSnapshot.overlays must contain wind, rain, and temperature keys

## SPECIFICATION

**Objective:** Update the planRideOrchestrator to propagate rain and temperature overlays from the weather provider through to the route options.

**Success looks like:** routeSnapshot.overlays has wind, rain, and temperature data. Partial weather failure (e.g., rain fetch fails but wind succeeds) does not block the pipeline.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Weather provider returns wind, rain, and temperature overlays | Orchestrator processes conditions step | routeSnapshot.overlays contains all three overlay types | `npx vitest run convex/actions/agent/lib/planRideOrchestrator.test.ts` |
| 2 | Weather provider returns wind only (rain/temp fail) | Orchestrator processes conditions step | routeSnapshot.overlays has wind; rain and temperature are undefined but pipeline continues | `npx vitest run convex/actions/agent/lib/planRideOrchestrator.test.ts` |
| 3 | Route options are built from results | buildOptionsFromResults processes overlays | All overlay data is available on PlannedRouteOptionView | `npx vitest run convex/actions/agent/lib/planRideOrchestrator.test.ts` |

## TEST CRITERIA

| # | Statement | Pass |
|---|-----------|------|
| 1 | routeSnapshot.overlays has wind, rain, temperature when all succeed | [ ] |
| 2 | Partial weather failure does not block pipeline | [ ] |
| 3 | buildOptionsFromResults passes all overlay data through | [ ] |
| 4 | Existing wind-only behavior unchanged | [ ] |

## GUARDRAILS

### WRITE-ALLOWED
- convex/actions/agent/lib/planRideOrchestrator.ts (MODIFY)
- convex/actions/agent/lib/planRideOrchestrator.test.ts (MODIFY or NEW)

### WRITE-PROHIBITED
- convex/actions/agent/providers/weatherProvider.ts — changed in US-022
- Any UI files

## DESIGN

### References
- PRD 07-technical-backend.md §5.4
- convex/actions/agent/lib/planRideOrchestrator.ts (current implementation)

### Interaction Notes
- The orchestrator already has a conditions step — extend it to handle the new overlay types
- buildOptionsFromResults needs to propagate all overlay data to the view model

## CODING STANDARDS

- Deterministic logic only
- Graceful degradation: missing overlays should be undefined, not throw

## DEPENDENCIES

- US-022: Extend weatherProvider to fetch rain + temperature alongside wind

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | convex/actions/agent/lib/planRideOrchestrator.ts | Current orchestrator flow |
| Before implementation | US-022 | Understanding new overlay data structure |

## NOTES

This is a lightweight wiring task. The heavy lifting was done in US-022. The key concern is graceful degradation when weather data is partially available.
