================================================================================
TASK: AI-003 - Migrate 6 agent call sites to getAgentModel() + update tests
================================================================================

TASK_TYPE: FEATURE
STATUS: ✅ Completed (superseded by AI-002+AI-003 combined task)
COMPLETED: 2026-04-11T16:51:42Z
COMMIT: c6fd13c28e93e4ece884da24b8dca82e35f8b0ec
REVIEWER: convex-reviewer
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Problem:** Six files still call `getModel('anthropic', ...)`, `getModel('openai', AI_MODEL)`, or reference `AI_MODEL` for metadata. After AI-001 lands the registry and AI-002 strips env exports, these call sites break type-check until they migrate.

**Why it matters:** The whole point of Epic 0 is to make provider selection a one-file change. Until every call site routes through `getAgentModel()`, the old hardcoded strings can drift back in.

**Current state:** (post AI-001 + AI-002)
- `convex/actions/agent/lib/models.ts` exports `getAgentModel` / `getAgentModelInfo` ✅
- `convex/lib/env.ts` no longer exports `AI_MODEL`/`AI_PROVIDER` → type errors in files below
- Six call sites still use the old API

**Desired state:** Every agent file uses `getAgentModel('low' | 'high')`. `sendMessage.ts` uses `getAgentModelInfo('high').model` for metadata. `routingAgent.ts` key-check references `CEREBRAS_API_KEY` (or better: trusts the registry — see proposal Step 4 note). Tests mock `CEREBRAS_API_KEY` instead of `ANTHROPIC_API_KEY`.

**Reference:** `.spec/proposals/cerebras-provider-migration.md` — Steps 3, 4, 5.

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA (TDD Beads)
--------------------------------------------------------------------------------

AC-1: High-reasoning agents use getAgentModel('high')
  GIVEN: `orchestrator.ts` and `generateTripPlan.ts` are imported
  WHEN: a grep scans each file for `getModel(`
  THEN: no direct `getModel(` calls remain; `getAgentModel('high')` appears in each file — verified by a small vitest that reads the file contents and asserts absence/presence of these strings

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: convex/actions/agent/__tests__/provider-migration.test.ts
  TEST_FUNCTION: test_high_reasoning_files_use_getAgentModel_high

AC-2: Low-reasoning sub-agents use getAgentModel('low')
  GIVEN: `routingAgent.ts`, `searchAgent.ts`, `enrichmentAgent.ts`, `tools/enrichRoute.ts` are imported
  WHEN: grep scans each file for `getModel(` and for `getAgentModel('low')`
  THEN: no direct `getModel(` calls remain; each file calls `getAgentModel('low')` exactly once — verified as above

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: convex/actions/agent/__tests__/provider-migration.test.ts
  TEST_FUNCTION: test_low_reasoning_files_use_getAgentModel_low

AC-3: sendMessage metadata reads from getAgentModelInfo
  GIVEN: `sendMessage.ts` is imported
  WHEN: grep scans it for `AI_MODEL` and for `getAgentModelInfo`
  THEN: `AI_MODEL` no longer appears; `getAgentModelInfo('high').model` (or `.provider`) is the new metadata source

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: convex/actions/agent/__tests__/provider-migration.test.ts
  TEST_FUNCTION: test_sendMessage_uses_getAgentModelInfo

AC-4: ridePlanningAgent test uses CEREBRAS_API_KEY mock
  GIVEN: `ridePlanningAgent.test.ts` is run with a fresh env
  WHEN: the test suite executes
  THEN: the env mock sets `CEREBRAS_API_KEY`, the suite passes green, and no assertion references `ANTHROPIC_API_KEY` or a claude model ID

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: convex/actions/agent/__tests__/ridePlanningAgent.test.ts
  TEST_FUNCTION: (existing tests — assertion update + env mock swap)

Quality Criteria:
- [ ] All tests pass
- [ ] Lint passes
- [ ] Type check passes cleanly (no leftover `AI_MODEL`/`AI_PROVIDER` errors)
- [ ] RED evidence captured

--------------------------------------------------------------------------------
TEST CRITERIA (Boolean Verification)
--------------------------------------------------------------------------------

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | orchestrator.ts and generateTripPlan.ts each call getAgentModel('high') exactly once and contain no `getModel(` calls when scanned | AC-1 | `npx vitest run convex/actions/agent/__tests__/provider-migration.test.ts` | [ ] TRUE [ ] FALSE |
| 2 | routingAgent.ts, searchAgent.ts, enrichmentAgent.ts, tools/enrichRoute.ts each call getAgentModel('low') exactly once and contain no `getModel(` calls when scanned | AC-2 | `npx vitest run convex/actions/agent/__tests__/provider-migration.test.ts` | [ ] TRUE [ ] FALSE |
| 3 | sendMessage.ts references getAgentModelInfo and does not reference AI_MODEL when scanned | AC-3 | `npx vitest run convex/actions/agent/__tests__/provider-migration.test.ts` | [ ] TRUE [ ] FALSE |
| 4 | ridePlanningAgent.test.ts passes when only CEREBRAS_API_KEY is set in the env mock | AC-4 | `npx vitest run convex/actions/agent/__tests__/ridePlanningAgent.test.ts` | [ ] TRUE [ ] FALSE |
| 5 | Project type-check succeeds with zero errors after migration | all | `npx tsc --noEmit` | [ ] TRUE [ ] FALSE |
| 6 | Grep for `claude-sonnet`, `claude-haiku`, `getModel('anthropic'`, `getModel('openai'` across convex/actions/agent returns zero hits | all | `grep -rnE "claude-(sonnet\|haiku)\|getModel\('(anthropic\|openai)'" convex/actions/agent` | [ ] TRUE [ ] FALSE |

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. .spec/proposals/cerebras-provider-migration.md
   - Sections: "Implementation → Step 3, 4, 5", file migration table
   - Focus: Intelligence level per file, before/after code pattern, env mock change

2. convex/actions/agent/lib/models.ts (produced by AI-001)
   - Lines: ALL
   - Focus: Exact function signatures to import

3. convex/actions/agent/agents/orchestrator.ts
   - Lines: 370-390
   - Focus: Current `getModel` call to replace

4. convex/actions/agent/sendMessage.ts
   - Lines: 70-90
   - Focus: `AI_MODEL` metadata usage

5. convex/actions/agent/__tests__/ridePlanningAgent.test.ts
   - Lines: ALL
   - Focus: Env mock setup (vi.stubEnv, process.env patches) and any assertions touching model names

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED:
- convex/actions/agent/agents/orchestrator.ts (MODIFY)
- convex/actions/agent/agents/routingAgent.ts (MODIFY)
- convex/actions/agent/agents/searchAgent.ts (MODIFY)
- convex/actions/agent/agents/enrichmentAgent.ts (MODIFY)
- convex/actions/agent/generateTripPlan.ts (MODIFY)
- convex/actions/agent/tools/enrichRoute.ts (MODIFY)
- convex/actions/agent/sendMessage.ts (MODIFY)
- convex/actions/agent/__tests__/ridePlanningAgent.test.ts (MODIFY)
- convex/actions/agent/__tests__/provider-migration.test.ts (NEW)

WRITE-PROHIBITED:
- convex/actions/agent/lib/models.ts — owned by AI-001
- convex/lib/env.ts — owned by AI-002
- Any file not in WRITE-ALLOWED

MUST:
- [ ] Use `getAgentModel('high')` ONLY for orchestrator + generateTripPlan
- [ ] Use `getAgentModel('low')` for routingAgent, searchAgent, enrichmentAgent, enrichRoute
- [ ] Use `getAgentModelInfo('high').model` for sendMessage metadata
- [ ] Update `routingAgent.ts` key check: remove ANTHROPIC_API_KEY throw, (a) reference CEREBRAS_API_KEY OR (b) delete the check entirely per proposal's "validate in models.ts" suggestion
- [ ] Update `ridePlanningAgent.test.ts` env mock to `CEREBRAS_API_KEY`
- [ ] Each file should have only ONE `getAgentModel(...)` call (no duplication)

MUST NOT:
- [ ] Introduce any new `claude-*` or `gpt-*` string literals
- [ ] Change any agent's business logic — this is a mechanical refactor
- [ ] Modify `models.ts` or `env.ts`
- [ ] Touch `enrichRoute.test.ts` — per proposal, keep `OPENAI_API_KEY` mock there (enrichRoute still uses OpenAI for other things; double-check during implementation and skip the test change if enrichRoute.ts no longer imports OpenAI)

--------------------------------------------------------------------------------
CODE PATTERN (Reference)
--------------------------------------------------------------------------------

```typescript
// BEFORE (orchestrator.ts)
import { getModel } from '@mariozechner/pi-ai'
const model = getModel('anthropic', 'claude-sonnet-4-6' as any)

// AFTER
import { getAgentModel } from '../lib/models'
const model = getAgentModel('high')
```

```typescript
// BEFORE (sendMessage.ts)
import { AI_MODEL } from '../../lib/env'
// ...
model: AI_MODEL

// AFTER
import { getAgentModelInfo } from './lib/models'
// ...
model: getAgentModelInfo('high').model
```

```typescript
// provider-migration.test.ts (new, file-scan style)
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'
import { describe, test, expect } from 'vitest'

const read = (p: string) => readFileSync(resolve(__dirname, '..', p), 'utf8')

describe('provider migration', () => {
  test('orchestrator uses getAgentModel high', () => {
    const src = read('agents/orchestrator.ts')
    expect(src).not.toMatch(/getModel\(/)
    expect(src).toContain("getAgentModel('high')")
  })
  // ... repeat per file
})
```

--------------------------------------------------------------------------------
AGENT INSTRUCTIONS (TDD Flow)
--------------------------------------------------------------------------------

AGENT: convex-implementer

## FOR EACH ACCEPTANCE CRITERION:

### RED PHASE
  READ: Current AC, proposal Steps 3-5, the target file(s) to migrate
  WRITE: One test in `provider-migration.test.ts` (or update `ridePlanningAgent.test.ts` for AC-4)
  RUN: npx vitest run convex/actions/agent/__tests__/provider-migration.test.ts --reporter=verbose
  VERIFY: Test FAILS because the target files still use the old API
  RETURN: { phase: "RED", test_file, test_function, failure_output }

### GREEN PHASE
  WRITE: Mechanical refactor of the target file(s) for this AC — replace the getModel call, import from registry
  RUN: npx vitest run convex/actions/agent/__tests__/ --reporter=verbose
  VERIFY: New test PASSES; existing agent tests still pass
  RETURN: { phase: "GREEN", files_changed, test_output }

### REFACTOR PHASE
  WRITE: Tidy imports, remove dead `AI_MODEL`/`AI_PROVIDER` references, keep behavior identical
  RUN: npx vitest run convex/actions/agent/__tests__/ && npx tsc --noEmit
  VERIFY: Both clean

## AFTER ALL ACs COMPLETE:
  Orchestrator dispatches convex-reviewer

--------------------------------------------------------------------------------
ORCHESTRATOR VERIFICATION PROTOCOL
--------------------------------------------------------------------------------

AFTER RED PHASE (per AC):
  RUN: npx vitest run convex/actions/agent/__tests__/provider-migration.test.ts --reporter=verbose
  EXPECT: Exit != 0 with the new test failing
  IF PASS: Reject "Vanity test"

AFTER GREEN PHASE (per AC):
  RUN: npx vitest run convex/actions/agent/__tests__/ --reporter=verbose
  EXPECT: Exit 0

AFTER REFACTOR PHASE (final):
  RUN: npx vitest run convex/ && npx tsc --noEmit
  EXPECT: Exit 0 for both

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: convex-implementer
**Rationale**: Mechanical refactor across Convex agent files. Low risk, high volume of small edits — exactly what this implementer is scoped for.

**Review Agent**: convex-reviewer
**Rationale**: Verify no business-logic drift, confirm every call site routes through the registry, and check that tests still meaningfully exercise the agents.

**Assignment Date**: 2026-04-11

--------------------------------------------------------------------------------
EVIDENCE GATES
--------------------------------------------------------------------------------

Gate 1: All Tests Pass
  Command: npx vitest run convex/
  Expected: Exit 0

Gate 2: Type Check
  Command: npx tsc --noEmit
  Expected: Exit 0

Gate 3: Lint
  Command: npx eslint convex/actions/agent
  Expected: Exit 0

Gate 4: Zero hardcoded provider/model strings
  Command: grep -rnE "claude-(sonnet|haiku)|getModel\('(anthropic|openai)'" convex/actions/agent
  Expected: Exit 1 (no matches)

Gate 5: Scope Compliance
  Command: git diff --name-only
  Expected: Only files in WRITE-ALLOWED

Gate 6: RED Phase Evidence
  Required: Task comments show each test failed before implementation

--------------------------------------------------------------------------------
REVIEW CRITERIA (for convex-reviewer)
--------------------------------------------------------------------------------

TDD Quality:
- [ ] One test per AC (file-scan tests are acceptable here — the behavior is the migration itself)
- [ ] RED evidence captured
- [ ] No vanity tests

Code Quality:
- [ ] Every target file imports from `../lib/models` (or `./lib/models`) — no direct `@mariozechner/pi-ai` import for `getModel`
- [ ] `sendMessage.ts` metadata path uses `getAgentModelInfo`, not a hardcoded string
- [ ] `routingAgent.ts` key-check is consistent (either CEREBRAS_API_KEY or delegated to models.ts)
- [ ] No agent's business behavior changed

Domain-Specific:
- [ ] `orchestrator.ts` still uses 'high' (intent classification)
- [ ] Sub-agents still use 'low' (focused tool calls)
- [ ] `generateTripPlan.ts` uses 'high' (multi-step generation)

Security:
- [ ] No API keys logged
- [ ] No API keys committed

Review Verdict: [ ] APPROVED   [ ] NEEDS_FIXES

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- AI-001 — imports `getAgentModel` / `getAgentModelInfo`
- AI-002 — removes `AI_MODEL`/`AI_PROVIDER` so migration compiles cleanly

Blocks:
- Epic 2 PIPE-004 (LLM extraction) and CONVEX-006 (intent extraction) — both should consume the registry once it's in place

--------------------------------------------------------------------------------
TASK READINESS
--------------------------------------------------------------------------------

Prerequisites:
- [ ] AI-001 merged
- [ ] AI-002 merged
- [ ] `CEREBRAS_API_KEY` set in the dev Convex deployment

Can Execute In Parallel With: (none — terminal task in epic 0)

--------------------------------------------------------------------------------
NOTES
--------------------------------------------------------------------------------

- `tools/enrichRoute.ts` is the most suspect call site: today it passes `AI_MODEL` (a Claude model ID) to the `openai` provider. That's a pre-existing bug. This task fixes it by routing through `getAgentModel('low')`. Double-check that the tool's behavior (label generation) still works with `zai-glm-4.7` — if it demands structured outputs, note it and escalate before GREEN phase.
- The Boy Scout Rule applies: if the mechanical refactor surfaces a latent type error in one of these files, fix it in-flight rather than leaving it for later.
- Per proposal Step 4, prefer moving key validation into `models.ts` at module load. If that's done here, update AI-001's WRITE-ALLOWED rule via a follow-up comment — or fold the key check into `routingAgent.ts` for simplicity (proposal explicitly allows either).

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: convex-reviewer (Claude Opus 4.6)
Date: 2026-04-11

================================================================================
