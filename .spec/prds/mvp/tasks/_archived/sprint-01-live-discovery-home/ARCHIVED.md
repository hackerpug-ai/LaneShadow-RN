# ARCHIVED — Sprint 01 "Live Discovery Home" (deprecated 2026-06-15)

This sprint's task files describe the **dedicated / standalone discovery** approach
(suggestion *pills* + dedicated-screen teardown + filter-bar/sort framing) that was
**deprecated by PRD v3.0.0** when the *separate discovery view was removed*.

Under PRD v3.0.0 discovery is the behavior of the **route plan view** (`index.tsx`):
curated-route **suggestion cards over the chat input** + **chat-driven NL curated
discovery** (no dedicated screen, no filter-bar / sort-toggle / by-state picker).

Some of this scope was built (and partly built poorly) and now lives in the plan view.
The replanned roadmap re-homes the salvageable scope into a new plan-view–centric
Sprint 01. These files are preserved as the as-built historical record only — **do not
execute them**. See `../../ROADMAP.md` and `../../05-uc-disc.md`.
