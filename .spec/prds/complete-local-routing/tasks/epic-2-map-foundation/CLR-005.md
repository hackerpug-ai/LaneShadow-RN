# CLR-005: Install Mapbox SDK Dependencies

> Status: ✅ Completed
> Completed: 2026-04-10T05:28:18Z
> Commit: 8d323a40838e997ae15b94f1dd4408cff916fc5f
> Reviewer: feature-dev:code-reviewer

**Task ID:** CLR-005
**Epic:** Epic 2 - Map Foundation
**Assigned To:** react-native-ui-implementer
**Estimate:** 60 min
**Type:** [FEATURE]

---

## CRITICAL CONSTRAINTS

1. **MUST** install @rnmapbox/maps version 10.1.0 or higher - earlier versions lack React Native 0.73+ support
2. **NEVER** modify existing Google Maps imports until Mapbox implementation is complete and tested - parallel installation required
3. **STRICTLY** follow React Native + Expo installation instructions from @rnmapbox/maps documentation
4. **MUST** configure both iOS and Android native dependencies - map rendering requires native modules
5. **NEVER** commit access tokens to repository - always use environment variables

---

## SPECIFICATION

**Objective:** Install and configure @rnmapbox/maps SDK with all required dependencies for React Native and Expo.

**Success State:** @rnmapbox/maps is installed, dependencies are linked, and project builds successfully on both iOS and Android platforms. No build errors or dependency conflicts exist.

---

## ACCEPTANCE CRITERIA

### AC-001: Package Installation
**GIVEN** LaneShadow project with existing dependencies
**WHEN** developer runs npm install command
**THEN** system installs @rnmapbox/maps@^10.1.0 package
**AND** system installs all peer dependencies automatically
**AND** package.json contains @rnmapbox/maps entry

**Verify:** Run `npm install @rnmapbox/maps@^10.1.0`, check package.json for entry, verify no dependency conflicts.

### AC-002: iOS Pod Installation
**GIVEN** @rnmapbox/maps package is installed
**WHEN** developer runs pod install command in iOS directory
**THEN** system installs Mapbox native SDK pods
**AND** Podfile.lock is updated with Mapbox dependencies
**AND** no pod installation errors occur

**Verify:** Run `cd ios && pod install`, check Podfile.lock for Mapbox entries, verify successful installation.

### AC-003: Android Gradle Configuration
**GIVEN** @rnmapbox/maps package is installed
**WHEN** developer builds Android project
**THEN** system includes Mapbox SDK in Gradle dependencies
**AND** build completes without Gradle errors
**AND** Mapbox native libraries are linked correctly

**Verify:** Run `npm run android`, check build output for Mapbox entries, verify successful build.

### AC-004: Expo Plugin Configuration
**GIVEN** project uses Expo (as per PRD)
**WHEN** app.json or app.config.js is configured
**THEN** system includes @rnmapbox/maps in Expo plugins array
**AND** system provides Mapbox access tokens via plugin config
**AND** Expo dev client builds successfully with Mapbox

**Verify:** Check app.config.js for rnmapbox plugin entry, run `expo prebuild --clean`, verify successful build.

### AC-005: TypeScript Type Definitions
**GIVEN** @rnmapbox/maps is installed
**WHEN** TypeScript performs type checking
**THEN** system recognizes Mapbox component types
**AND** no TypeScript errors occur for Mapbox imports
**AND** IDE autocomplete shows Mapbox components and props

**Verify:** Run `npm run typecheck`, import MapView component, verify type definitions are available.

---

## TEST CRITERIA

- [ ] @rnmapbox/maps@^10.1.0 installed in package.json
- [ ] No dependency conflicts in npm ls output
- [ ] iOS pods install successfully
- [ ] Android builds successfully with Gradle
- [ ] Expo plugin configured in app.config.js
- [ ] TypeScript recognizes Mapbox types
- [ ] Project builds on iOS simulator
- [ ] Project builds on Android emulator
- [ ] No build warnings related to dependencies
- [ ] Mapbox access tokens not committed to repository

---

## GUARDRAILS

**WRITE-ALLOWED FILES:**
- `package.json` - Add @rnmapbox/maps dependency
- `app.config.js` - Add Expo plugin configuration
- `ios/Podfile` - Modify for Mapbox pod installation if needed
- `android/build.gradle` - Add Mapbox Maven repository if needed
- `android/app/build.gradle` - Add Mapbox SDK dependency if needed
- `.gitignore` - Ensure tokens not committed

**NEVER MODIFY:**
- Existing Google Maps implementation (keep parallel during migration)
- `styles/RULES.md` - Theme system is read-only
- Existing app navigation structure

---

## DESIGN

**References:**
- @rnmapbox/maps Documentation: https://github.com/rnmapbox/maps
- Expo Plugin Docs: https://docs.expo.dev/guides/rnmapbox/
- PRD Phase 1: Foundation - "Install Dependencies" section

**Patterns:**
- NPM package installation with version pinning (^10.1.0)
- Expo plugin configuration in app.config.js
- Environment variable usage for access tokens
- CocoaPods for iOS native dependencies
- Gradle for Android native dependencies

**Anti-Patterns:**
- **DO NOT** commit Mapbox access tokens to repository
- **DO NOT** uninstall Google Maps until migration complete
- **DO NOT** modify existing map components yet
- **DO NOT** skip iOS pod installation step
- **DO NOT** use unstable Mapbox SDK versions

**Installation Commands:**
```bash
npm install @rnmapbox/maps@^10.1.0
cd ios && pod install
npm run android  # Verify Android build
```

---

## VERIFICATION GATES

### Pre-Commit Verification
```bash
# Verify package installation
npm ls @rnmapbox/maps

# Check for dependency conflicts
npm ls --depth=0

# TypeScript type checking
npm run typecheck

# iOS build verification
npm run ios

# Android build verification
npm run android
```

### Platform Testing Checklist
1. Install package, verify no npm conflicts
2. Run `cd ios && pod install`, verify success
3. Build iOS project, verify no errors
4. Build Android project, verify no Gradle errors
5. Configure Expo plugin in app.config.js
6. Verify TypeScript recognizes Mapbox types
7. Test on iOS simulator (should build, no map rendering yet)
8. Test on Android emulator (should build, no map rendering yet)

### Dependency Verification
- No duplicate React Native versions
- No conflicting peer dependency warnings
- All native dependencies linked correctly
- Expo dev client builds successfully

---

## SUCCESS METRICS

- Installation time: < 10 minutes
- Build success rate: 100% (iOS and Android)
- Dependency conflicts: 0
- Type definition coverage: 100%

---

## NOTES

**Next Steps:** After this task, proceed to CLR-006 (Configure Expo Plugin) to set up Mapbox access tokens and complete SDK configuration.

**Access Token Setup:**
- Sign up for Mapbox account: https://www.mapbox.com/
- Create access token with downloads:read scope
- Store token in Convex environment variables
- Reference token in app.config.js via process.env

**Version Selection:**
- @rnmapbox/maps@^10.1.0 is minimum required version
- Supports React Native 0.73+ and Expo 50+
- Check for newer stable versions before installing

**Known Issues:**
- May need to increase Android Gradle memory for large SDK
- iOS may require deployment target 13.0+
- Check @rnmapbox/maps release notes for platform-specific issues

**Dependencies:**
- None (this is the first task in Epic 2)
