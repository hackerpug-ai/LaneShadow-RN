================================================================================
TASK: BASE-009a - Crawl Plan Protocol framework + MotorcycleRoads remediation
================================================================================

TASK_TYPE: INFRA / REMEDIATION
STATUS: Done
TDD_PHASE: GREEN
CURRENT_AC: complete
PRIORITY: P0
EFFORT: M
TYPE: REMEDIATION
ITERATION: 1
ESTIMATED_EFFORT_MINUTES: 330

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Build the shared `scripts/curation/pipeline/sources/crawl_plan/` framework module (`inventory.py`, `selectors.py`, `parser.py`, `executor.py`, `__init__.py`) with dedicated unit tests. This framework is consumed by BASE-009b (BBR remediation), Epic 4 SRC-001/006, and Sprint 9 RID-001/002/006 — it MUST be generic, not MR-specific.
MUST: Apply all seven phases of [CRAWL-PLAN-PROTOCOL.md](../CRAWL-PLAN-PROTOCOL.md) to motorcycleroads.com (Form A — HTML scraper). Each phase's deliverable must be committed before the next phase runs.
MUST: Use the Phase 0 `site-map.md` pre-committed by the user (manual recon, not agent-driven). If the committed site-map.md is missing or empty at task start, STOP and escalate.
MUST: Replace `staging/motorcycleroads.jsonl` with the protocol-driven executor output. The new file has row count matching the committed `urls.jsonl` inventory within ±10%.
MUST: Commit `crawl-plans/motorcycleroads/crawl-report.md` with verdict PASS before this task is marked done. If the honest verdict is PASS WITH ISSUES or FAIL, STOP and escalate to the user.
NEVER: Copy selectors from the existing `scripts/curation/pipeline/sources/motorcycleroads.py`. The existing selectors (`.field-field-rating`, `[class*='rating']`, `article.route .content`, `.field-field-scenery`) are the failure mode this task exists to fix. New selectors MUST be derived in Phase 3 against committed fixtures with `fixture_yield` validation.
NEVER: Touch any BestBikingRoads artifact, staging file, or crawl-plan directory. BBR remediation is BASE-009b scope.
NEVER: Regenerate `baseline/catalog.jsonl`, `baseline/scores.json`, `baseline/archetype_counts.json`, `baseline/source_counts.json`, `baseline-report.md`, or `review.md`. Those artifacts are regenerated by BASE-009b AFTER BBR also lands — upgrading the Epic 2 verdict requires both sources clean.
NEVER: Mark this task done while any Phase 6 required-field yield is below 100% for fields marked `required: true`.
NEVER: Run Phase 5 execution before Phase 4 fixture tests pass locally. Running execution without committed, passing fixture tests is a protocol violation.
STRICTLY: Respect robots.txt and rate limits (MR: 2-3s delay) — same discipline as the current scraper's `base_scraper.py`.
STRICTLY: Framework code must be **generic**. It must work as-is for BASE-009b (BBR) without modification, and must be extensible to Form B (GIS JSON), Form C (RSS), Form D (paginated auth API) via adapter layers. The design review criteria specifically check this.

CROSS-CUTTING RULES (from 2026-04-13 Phase 0 recon — see DECISIONS.md "Phase 0 recon findings"):

MUST: Use `state_primary` (URL-derived) + `states_all` (DOM-parsed list) schema for route records. The previously-assumed single-string `state` field breaks on multi-state routes like Natchez Trace Parkway (crosses AL/MS/TN). Phase 4 fixture tests MUST assert `expected.state in record.states_all` (list membership), never `record.state == expected.state` (string equality). This rule applies to the framework schema, not just MR — BASE-009b (BBR), Epic 4 SRC-001/006, and Sprint 9 RID-001/002/006 all inherit it. See `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` for the Natchez Trace evidence.

MUST: `canonicalize(url)` MUST preserve path, query, and fragment case. Lowercase ONLY the scheme and host. MotorcycleRoads happens to have lowercase slugs (so this rule doesn't bite MR directly), but BestBikingRoads has mixed-case slugs like `Columbia-2`, and the framework is shared — so the path-case rule is framework-wide. Lowercasing the path would collapse `/alabama/Columbia-2` and `/alabama/columbia-2` into a single canonical URL and erase distinct routes. Example signature: `canonicalize(url) -> url with scheme+host lowercased, trailing slash stripped, path/query/fragment case preserved`.

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective:** Build the reusable `crawl_plan/` framework module and prove it end-to-end on MotorcycleRoads as the "easy case" before BASE-009b tackles BestBikingRoads. This task is the battle-test for the framework: if the framework has bugs, they surface here during MR's ~30-minute Phase 5 crawl, not during BBR's ~3.75-hour Phase 5 crawl.

**Why split from BASE-009b:** MR has **~2,044 US routes** (revised from the initial 300-1000 estimate after 2026-04-13 Phase 0 recon — the true master index is `/motorcycle-rides-in/united-states` with 103 paginated pages, NOT `/motorcycle-roads/{state}` which returns the homepage with a 30-route global sidebar and was the root cause of Epic 2's 30-route MR yield). MR's Phase 5 takes ~85 minutes at rate limit. BBR has ~4,100-4,500 routes with ~3.75 hour Phase 5 execution. MR is still the smaller and faster of the two, and framework bugs surfaced on MR's ~85-minute cycle cost ~85 minutes to diagnose and re-run, vs 3.75 hours on BBR. This task isolates the framework risk to the smaller case.

**In scope:**
1. Shared `scripts/curation/pipeline/sources/crawl_plan/` framework module (generic, reusable, unit-tested)
2. All seven protocol phases applied to motorcycleroads.com (Phase 0 user-provided, Phase 1-6 in this task)
3. Rewrite of `scripts/curation/pipeline/sources/motorcycleroads.py` as a thin glue layer over the framework
4. Committed crawl plan artifacts at `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/`
5. Committed fixtures at `fixtures/motorcycleroads/`
6. Committed Phase 4 contract tests for the framework AND for MR
7. Regenerated `staging/motorcycleroads.jsonl` from the new executor with `.audit.json` counters

**Out of scope (belongs to BASE-009b):**
- Any BestBikingRoads work
- Regeneration of `baseline/catalog.jsonl`, `baseline/scores.json`, `baseline/archetype_counts.json`, `baseline/source_counts.json`
- Rewriting `review.md` verdict (the upgrade from PASS WITH ISSUES to PASS requires both sources clean)
- Updating the Community Scrapers section of `baseline-report.md`

**Success looks like:**
1. `from scripts.curation.pipeline.sources.crawl_plan import inventory, selectors, parser, executor` works
2. Framework unit tests at `scripts/curation/tests/sources/test_crawl_plan_framework.py` pass
3. `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/` has 4 files committed: `site-map.md` (user-provided), `urls.jsonl`, `selectors.yaml`, `crawl-report.md` (verdict PASS)
4. `fixtures/motorcycleroads/` has ≥3 fixtures per page type + `fixtures.manifest.yaml` with `expected:` values
5. `scripts/curation/tests/sources/test_motorcycleroads_fixtures.py` exists and passes locally
6. `staging/motorcycleroads.jsonl` has 300-1000 rows matching the committed inventory within ±10%; `.audit.json` sibling has non-zero `fetched` and `parse_success`, zero `schema_validation_fail`
7. No Alabama-stamped Blue Ridge Parkway or Beartooth Pass entries (fixture test asserts `record.state == fixture.expected.state`)
8. `scripts/curation/pipeline/sources/motorcycleroads.py` is now a thin glue file (≤100 lines) that delegates discovery, parsing, and execution to the crawl_plan framework

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Why this task exists:** Epic 2's BASE-002 (community scrapers validation) produced a "PASS WITH ISSUES" verdict with MR yielding only 30 routes against a >50 expected baseline (true universe is ~300-1000). The MR scraper stamped "Blue Ridge Parkway" and "Beartooth Pass" as Alabama routes because sidebar cross-state links on state listing pages were treated as inbound links for the current state. Selectors like `.field-field-rating` and `[class*='rating']` were blind guesses never validated against a real page. See `review.md`, `baseline-report.md` (lines 133-211), and the 2026-04-13 evening DECISIONS.md entry "Crawl Plan Protocol adoption" for the full failure-mode analysis.

**Why split into 009a + 009b:** The single-task version of BASE-009 bundled framework build + MR + BBR into one ~8-10 hour task. Splitting by source isolates risk: framework bugs found on MR cost 30 minutes to diagnose; the same bugs found on BBR cost 3.75 hours. The user performs Phase 0 recon manually for both sites and commits both `site-map.md` files before 009a is dispatched, so the framework has clear input specs for both sources from the start even though only MR is executed here.

**Current state at task start:**
- `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` is committed (user-provided, manual recon)
- `.spec/prds/curation-hardening/crawl-plans/bestbikingroads/site-map.md` is also committed (for BASE-009b's use)
- No framework code exists
- `staging/motorcycleroads.jsonl` contains 30 routes from the junk baseline
- `staging/bestbikingroads.jsonl` contains 413 routes from the junk baseline (leave alone — BASE-009b replaces it)
- `review.md` verdict is "PASS WITH ISSUES" (leave alone — BASE-009b upgrades it)

**Desired state at task end:**
- Framework module committed, importable, unit-tested
- MR crawl plan artifacts committed with verdict PASS
- MR staging file replaced with clean data
- BBR artifacts untouched
- Epic 2 `review.md` untouched (still PASS WITH ISSUES pending BASE-009b)

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA (TDD Beads)
--------------------------------------------------------------------------------

AC-1: Shared crawl_plan framework module exists, importable, and unit-tested
  GIVEN: Task start (no framework exists)
  WHEN: `scripts/curation/pipeline/sources/crawl_plan/` is created with inventory.py, selectors.py, parser.py, executor.py, __init__.py, and `test_crawl_plan_framework.py` is written covering canonicalize(), classify(), parse_with_selectors(), and the executor's resume logic
  THEN: `from scripts.curation.pipeline.sources.crawl_plan import inventory, selectors, parser, executor` succeeds AND `pytest scripts/curation/tests/sources/test_crawl_plan_framework.py` exits 0

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "from scripts.curation.pipeline.sources.crawl_plan import inventory, selectors, parser, executor; print('ok')" && PYTHONPATH=$(pwd) .venv/bin/python -m pytest scripts/curation/tests/sources/test_crawl_plan_framework.py -v`

AC-2: Phase 0 MR site-map.md is committed (pre-existing input)
  GIVEN: Framework module from AC-1 exists
  WHEN: The Phase 0 site-map is checked
  THEN: `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` exists and contains page type taxonomy (PT-NN), URL patterns, sample URLs, and known traps section

  VERIFY: `f=".spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md"; test -f "$f" && grep -q "PT-" "$f" && grep -qiE "sample" "$f" && grep -qiE "trap" "$f" && echo "MR site-map PASS"`

AC-3: Phase 1 MR urls.jsonl committed with row count in expected range
  GIVEN: site-map.md from AC-2 defines page types and URL patterns (master index at `/motorcycle-rides-in/united-states` with 103 paginated pages — NOT `/motorcycle-roads/{state}`, which Phase 0 recon established is the homepage with a 30-route global sidebar and was the root cause of Epic 2's 30-route yield)
  WHEN: Inventory script runs against the MR master index (all 103 pages, paginated)
  THEN: `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/urls.jsonl` exists; route-detail count in [1800, 2200] (true universe ~2,044 per Phase 0 measurement); every row has non-null page_type

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "import json; rows=[json.loads(l) for l in open('.spec/prds/curation-hardening/crawl-plans/motorcycleroads/urls.jsonl')]; details=[r for r in rows if 'route-detail' in r['page_type']]; assert 1800 <= len(details) <= 2200, len(details); assert all(r['page_type'] for r in rows); print(f'MR inventory PASS: {len(details)} route details, {len(rows)} total rows')"`

AC-4: Phase 2 MR fixtures committed with manifest
  GIVEN: urls.jsonl from AC-3 has classified URLs
  WHEN: 3-5 MR fixtures per page type are downloaded and committed
  THEN: `fixtures/motorcycleroads/` contains directories per page type with ≥3 .html files and `fixtures.manifest.yaml` recording source URL, fetched_at, sha256, and at least `route_name` and `state` expected values per fixture

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "
import os, yaml
base = 'fixtures/motorcycleroads'
assert os.path.isdir(base)
m = yaml.safe_load(open(f'{base}/fixtures.manifest.yaml'))
for pt, items in m.items():
    assert len(items) >= 3, f'{pt} has {len(items)} fixtures'
    for it in items:
        assert os.path.isfile(f'{base}/{pt}/{it[\"file\"]}')
        assert 'expected' in it and 'route_name' in it['expected']
print('MR fixtures PASS')"`

AC-5: Phase 3 MR selectors.yaml committed with fixture_yield 5/5 on required fields
  GIVEN: Fixtures from AC-4 exist
  WHEN: Selectors are derived (LLM-assisted per protocol) and validated against all fixtures of each page type
  THEN: `.../crawl-plans/motorcycleroads/selectors.yaml` exists; every field with `required: true` has `fixture_yield` equal to the total fixture count for that page type

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "
import yaml
sel = yaml.safe_load(open('.spec/prds/curation-hardening/crawl-plans/motorcycleroads/selectors.yaml'))
for pt, fields in sel.items():
    for fname, fdef in fields.items():
        if fdef.get('required'):
            y = fdef.get('fixture_yield','0/0')
            n,d = map(int, y.split('/'))
            assert n==d and n>0, f'{pt}/{fname} required but yield {y}'
print('MR selectors PASS')"`

AC-6: Phase 4 MR fixture tests pass
  GIVEN: selectors.yaml from AC-5 and fixtures from AC-4
  WHEN: `pytest scripts/curation/tests/sources/test_motorcycleroads_fixtures.py` runs
  THEN: All assertions pass (exit 0), including the cross-field state assertion (`record.state == fixture.expected.state`)

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -m pytest scripts/curation/tests/sources/test_motorcycleroads_fixtures.py -v`

AC-7: Phase 5 MR execution replaces staging file with ≥90% inventory yield
  GIVEN: Fixture tests passing from AC-6
  WHEN: `python -m scripts.curation.pipeline.sources.motorcycleroads` runs under the new framework
  THEN: `staging/motorcycleroads.jsonl` row count ≥90% of inventory row count; `staging/motorcycleroads.jsonl.audit.json` shows fetched > 0, parse_success > 0, schema_validation_fail < 1% of parse attempts

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "
import json, os
staging = 'staging/motorcycleroads.jsonl'
audit = 'staging/motorcycleroads.jsonl.audit.json'
assert os.path.isfile(staging) and os.path.isfile(audit)
a = json.load(open(audit))
assert a.get('fetched',0) > 0 and a.get('parse_success',0) > 0
assert a.get('schema_validation_fail',0) / max(1, a.get('parse_success',1)) < 0.01
inv = sum(1 for _ in open('.spec/prds/curation-hardening/crawl-plans/motorcycleroads/urls.jsonl'))
staged = sum(1 for _ in open(staging))
assert staged / inv >= 0.90, f'yield {staged/inv:.1%}'
print(f'MR execution PASS: {staged}/{inv} = {staged/inv:.1%}')"`

AC-8: Phase 6 MR crawl-report.md committed with verdict PASS
  GIVEN: Execution complete from AC-7 with audit counters
  WHEN: crawl-report.md is written per the protocol template
  THEN: `.../crawl-plans/motorcycleroads/crawl-report.md` exists with verdict PASS, counters table, required-field yield table, and landmark presence check for MR-resident landmarks

  VERIFY: `f=".spec/prds/curation-hardening/crawl-plans/motorcycleroads/crawl-report.md"; grep -qE "^\*\*?Verdict:\*\*? PASS$" "$f" && echo "MR crawl-report PASS"`

AC-9: motorcycleroads.py is now a thin glue layer, not a bespoke scraper
  GIVEN: All prior phases complete
  WHEN: `scripts/curation/pipeline/sources/motorcycleroads.py` is inspected
  THEN: File is ≤100 lines, contains no CSS selectors, no BeautifulSoup calls, and delegates discovery + parsing + execution to the `crawl_plan` framework module

  VERIFY: `PYTHONPATH=$(pwd) .venv/bin/python -c "
src = open('scripts/curation/pipeline/sources/motorcycleroads.py').read()
lines = [l for l in src.split('\n') if l.strip() and not l.strip().startswith('#')]
assert len(lines) <= 100, f'{len(lines)} non-comment lines'
assert 'BeautifulSoup' not in src, 'BeautifulSoup reference in glue file'
assert 'soup.select' not in src, 'soup.select in glue file'
assert 'crawl_plan' in src, 'does not import crawl_plan framework'
print('MR glue PASS')"`

Quality Criteria:
- [ ] All 9 ACs verified
- [ ] Framework is generic (AC-1 unit tests prove it; design review verifies Form B/C/D extensibility)
- [ ] Phase 4 fixture tests committed BEFORE Phase 5 execution (verify via commit order)
- [ ] No `--no-verify` or bypass flags

--------------------------------------------------------------------------------
TEST CRITERIA (Boolean Verification)
--------------------------------------------------------------------------------

| # | Boolean Statement | Maps To AC | Status |
|---|-------------------|------------|--------|
| 1 | Framework modules importable + unit tests pass | AC-1 | [ ] TRUE [ ] FALSE |
| 2 | MR site-map.md committed with page types + traps | AC-2 | [ ] TRUE [ ] FALSE |
| 3 | MR urls.jsonl route-detail count in [1800, 2200] (master index basis) | AC-3 | [ ] TRUE [ ] FALSE |
| 4 | MR fixtures ≥3 per page type + manifest.expected | AC-4 | [ ] TRUE [ ] FALSE |
| 5 | MR selectors.yaml fixture_yield 5/5 on required fields | AC-5 | [ ] TRUE [ ] FALSE |
| 6 | MR fixture pytest tests pass | AC-6 | [ ] TRUE [ ] FALSE |
| 7 | MR staging yield ≥90% of inventory | AC-7 | [ ] TRUE [ ] FALSE |
| 8 | MR crawl-report.md verdict PASS | AC-8 | [ ] TRUE [ ] FALSE |
| 9 | motorcycleroads.py is ≤100 lines of thin glue | AC-9 | [ ] TRUE [ ] FALSE |

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. .spec/prds/curation-hardening/tasks/CRAWL-PLAN-PROTOCOL.md — ALL; focus on Form A deliverables, phase gates, tool recommendations
2. .spec/prds/curation-hardening/tasks/CURATION-REVIEW-PROTOCOL.md — Step 1 pre-verification gate (note: review.md upgrade is BASE-009b scope, not this task)
3. .spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md — user-provided Phase 0 input (required to exist before task start)
4. .spec/prds/curation-hardening/tasks/epic-02-baseline-pipeline-validation/DECISIONS.md — "Crawl Plan Protocol adoption" entry for the split rationale
5. scripts/curation/pipeline/sources/motorcycleroads.py — lines 112-250; the failure mode. DO NOT copy selectors.
6. scripts/curation/pipeline/sources/base_scraper.py — rate limiting + robots.txt + JSONL writing primitives to preserve

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED:
- scripts/curation/pipeline/sources/crawl_plan/ — NEW framework module (inventory, selectors, parser, executor, __init__)
- scripts/curation/pipeline/sources/motorcycleroads.py — REWRITE as thin glue (≤100 lines)
- .spec/prds/curation-hardening/crawl-plans/motorcycleroads/urls.jsonl — NEW (Phase 1)
- .spec/prds/curation-hardening/crawl-plans/motorcycleroads/selectors.yaml — NEW (Phase 3)
- .spec/prds/curation-hardening/crawl-plans/motorcycleroads/crawl-report.md — NEW (Phase 6)
- fixtures/motorcycleroads/ — NEW (Phase 2 HTML + manifest)
- scripts/curation/tests/sources/test_crawl_plan_framework.py — NEW unit tests
- scripts/curation/tests/sources/test_motorcycleroads_fixtures.py — NEW Phase 4 contract tests
- scripts/curation/tests/sources/conftest.py — NEW or UPDATE (fixture loader helper)

WRITE-PROHIBITED:
- scripts/curation/pipeline/sources/bestbikingroads.py — BASE-009b scope
- .spec/prds/curation-hardening/crawl-plans/bestbikingroads/ — BASE-009b scope
- fixtures/bestbikingroads/ — BASE-009b scope
- .spec/prds/curation-hardening/tasks/epic-02-baseline-pipeline-validation/baseline/** — BASE-009b scope
- .spec/prds/curation-hardening/tasks/epic-02-baseline-pipeline-validation/review.md — BASE-009b scope
- .spec/prds/curation-hardening/tasks/epic-02-baseline-pipeline-validation/baseline-report.md — BASE-009b scope
- convex/** — no schema changes
- BASE-000.md through BASE-008.md — frozen prior tasks
- CRAWL-PLAN-PROTOCOL.md — constitution, do not modify during task execution
- Any commit using `--no-verify`

MUST:
- [ ] Framework module has unit tests committed with AC-1
- [ ] Phase 4 fixture tests committed before Phase 5 execution (commit order matters)
- [ ] Executor respects existing `base_scraper.py` rate limit + robots.txt discipline
- [ ] Framework code is generic (design review checks extensibility to Form B/C/D)

MUST NOT:
- [ ] Touch BBR in any way
- [ ] Regenerate baseline files or review.md
- [ ] Reintroduce the Alabama-stamped Blue Ridge Parkway bug — AC-6 fixture test guards this

--------------------------------------------------------------------------------
CODE PATTERN (Reference)
--------------------------------------------------------------------------------

See `CRAWL-PLAN-PROTOCOL.md` "Forms A" section for the canonical framework skeleton (inventory.canonicalize(), classify(), discover(); parser.parse_with_selectors() with fail-closed SchemaViolation on required-field null; executor.run_crawl() with resume-from-progress-file; fixture loader pattern for contract tests).

**Anti-pattern:** Do NOT write a MR-specific inventory function. The framework's `discover()` must take patterns and a link extraction function as parameters, not hardcode MR's URL regex.

**Framework design validation:** Before Phase 5, mentally walk through how the framework would support:
- Form B: Koordinates GIS endpoint returning GeoJSON FeatureCollection → selectors.yaml with JSONPath expressions, parser adapter for JSON
- Form C: ADVRider RSS feed → selectors.yaml with XPath, parser adapter for feedparser output
- Form D: Reddit OAuth2 paginated API → executor adapter for cursor pagination

If any of the three feels like it would require rewriting the framework instead of adding an adapter, the design is MR-specific and must be refactored before AC-7 is marked complete.

--------------------------------------------------------------------------------
AGENT INSTRUCTIONS (Phase-Driven Flow)
--------------------------------------------------------------------------------

AGENT: python-implement

### Preflight (0 min — check inputs)
  VERIFY: `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` exists. If not, STOP — user has not done Phase 0 recon yet.

### Phase 1 — INVENTORY (~45 min)
  DO: Build `crawl_plan.inventory` module. Load patterns from site-map.md. Fetch MR's 50 state listing pages. Canonicalize, classify, dedupe. Reject unclassified links and log reject rate.
  COMMIT: urls.jsonl + inventory.py. If reject rate > 10%, STOP — return to user for Phase 0 revision.
  GATE: AC-3 verify passes.

### Phase 2 — FIXTURES (~30 min)
  DO: Download 3-5 real MR pages per page type. Use real landmark routes where possible (Tail of the Dragon if MR has it, otherwise any well-known twisty route). Write fixtures.manifest.yaml with sha256 and expected values.
  COMMIT: fixtures + manifest.
  GATE: AC-4 verify passes.

### Phase 3 — SELECTOR SPEC (~45 min, LLM-assisted)
  DO: For each page type, feed one fixture to Firecrawl /extract or GLM-4.7-flash. Validate proposed selectors across ALL fixtures. Record fixture_yield per field. Refine required fields until 5/5.
  WRITE: selectors.yaml + framework selectors.py.
  COMMIT.
  GATE: AC-5 verify passes.

### Phase 4 — DRY-RUN PARSE (~45 min, fast iteration)
  DO: Write test_crawl_plan_framework.py (unit tests) and test_motorcycleroads_fixtures.py (contract tests per the protocol pattern). Run pytest. Iterate parser.py until all tests pass.
  COMMIT: tests + parser.py.
  GATE: AC-1 + AC-6 pass.

### Phase 5 — EXECUTION (~30 min, rate-limited)
  DO: Rewrite motorcycleroads.py as ≤100-line glue. Build executor.py. Run `python -m scripts.curation.pipeline.sources.motorcycleroads`. Watch audit.json updates.
  HANDLE: schema_validation_fail → log, push to needs_llm_fallback.jsonl. If >5% fail rate, STOP.
  WRITE: staging/motorcycleroads.jsonl + .progress + .audit.json + new motorcycleroads.py.
  GATE: AC-7 + AC-9 pass.

### Phase 6 — ACCOUNTING (~15 min)
  DO: Write crawl-plans/motorcycleroads/crawl-report.md with counters, yields, landmark check, verdict PASS.
  COMMIT: crawl-report.md + any final framework tweaks in one commit.
  GATE: AC-8 pass.

### Task end
  REPORT: Summary of committed artifacts, staging row count, any schema_validation_fails or LLM fallbacks, and the MR Phase 5 wall clock. Await user approval before BASE-009b is dispatched.

--------------------------------------------------------------------------------
ORCHESTRATOR VERIFICATION PROTOCOL
--------------------------------------------------------------------------------

AFTER Phase 1: AC-3 verify → expect "MR inventory PASS"
AFTER Phase 2: AC-4 verify → expect "MR fixtures PASS"
AFTER Phase 3: AC-5 verify → expect "MR selectors PASS"
AFTER Phase 4: AC-1 + AC-6 verify → expect "ok" and pytest exit 0
AFTER Phase 5: AC-7 + AC-9 verify → expect "MR execution PASS" and "MR glue PASS"
AFTER Phase 6: AC-8 verify → expect "MR crawl-report PASS"
FINALLY: AC-2 verify (should have been true from task start, re-check for integrity)

If any gate fails, STOP. Do not skip ahead. Preserve the working tree; escalate to user with the failing output.

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent:** python-implement
**Review Agent:** python-review + code-reviewer (for framework design review — Form B/C/D extensibility check)
**Assignment Date:** 2026-04-13

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- BASE-008 (committed Epic 2 baseline with PASS WITH ISSUES verdict; the baseline this task starts from)
- CRAWL-PLAN-PROTOCOL.md (adopted 2026-04-13 PM)
- User-committed Phase 0: `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` (manual recon)
- User-committed Phase 0: `.spec/prds/curation-hardening/crawl-plans/bestbikingroads/site-map.md` (for BASE-009b — co-committed with MR site-map by the user before task dispatch)

Blocks:
- BASE-009b (BBR remediation uses the framework built here)

--------------------------------------------------------------------------------
TASK READINESS
--------------------------------------------------------------------------------

Prerequisites:
- [ ] BASE-008 committed
- [ ] CRAWL-PLAN-PROTOCOL.md committed
- [ ] DECISIONS.md "Crawl Plan Protocol adoption" entry committed
- [ ] User has committed `crawl-plans/motorcycleroads/site-map.md` (manual Phase 0)
- [ ] User has committed `crawl-plans/bestbikingroads/site-map.md` (also manual Phase 0 — co-input for BASE-009b)

Can Execute In Parallel With: none (serial Epic 2 remediation, precedes BASE-009b)

--------------------------------------------------------------------------------
NOTES
--------------------------------------------------------------------------------

- Estimated effort: ~330 minutes (~5.5 hours). Breakdown: framework build ~90 min; Phase 1 inventory ~45 min (103 pagination pages on the master index); Phase 2 fixtures ~30 min; Phase 3 selectors ~45 min; Phase 4 tests ~45 min; Phase 5 execution ~85 min (rate-limited, ~2,044 routes × 2.5s delay); Phase 6 ~15 min. Phase 5 budget revised upward from the initial ~30 min estimate after Phase 0 recon corrected the route count from 300-1000 to ~2,044 (see DECISIONS.md "Phase 0 recon findings").
- This is the FIRST application of CRAWL-PLAN-PROTOCOL.md. Implementation learnings feed back into the protocol as revision notes (separate commit, separate scope — do not bundle into the task commit).
- **Phase 0 recon is complete** (committed 2026-04-13 PM in `e7f6368`). The site-map.md at `.spec/prds/curation-hardening/crawl-plans/motorcycleroads/site-map.md` is the authoritative input and must not be modified by this task. Key findings the implementer must respect: (1) the master index URL is `/motorcycle-rides-in/united-states` (103 pages × ~20 routes), NOT `/motorcycle-roads/{state}` — the latter returns the homepage with a 30-route global rail and was the root cause of Epic 2's 30-route yield; (2) multi-state routes are first-class (Natchez Trace Parkway crosses AL/MS/TN), so the schema needs `state_primary` + `states_all`; (3) the discriminator between route detail (PT-03) and editorial trap (PT-05) is URL path depth — deterministic, not DOM-heuristic; (4) 7 page types identified total, 3 in-scope (PT-01 master listing, PT-02 state listing for coverage audit, PT-03 route detail).
- The framework's extensibility to Forms B/C/D is a DESIGN requirement, not just a NICE-TO-HAVE. code-reviewer's verdict checks this explicitly.
- After task completes, user reviews MR crawl-report.md + spot-checks 5 random records in staging/motorcycleroads.jsonl before dispatching BASE-009b. If framework bugs surface, they are fixed here (cheap re-run) before BBR execution (expensive re-run).

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
