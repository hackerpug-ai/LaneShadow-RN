================================================================================
TASK: SRC-006 - Ingest Rider Magazine 50 Best + preserve editorial provenance
================================================================================

TASK_TYPE: FEATURE
STATUS: Approved
TDD_PHASE: VERIFY_GREEN
CURRENT_AC: AC-5
PRIORITY: P1
EFFORT: M
TYPE: DEV
ITERATION: 1
ESTIMATED_EFFORT_MINUTES: 210
REVIEW_STATE: APPROVED
REVIEWED_AT: 2026-04-16
REMEDIATION_QUEUE:
- RM-1: Completed — added the missing Form A Phase 0 artifact `.spec/prds/curation-hardening/crawl-plans/rider_mag/site-map.md` and aligned it to the crawl-plan protocol.
- RM-2: Completed — wired `routeProvenance` through the real save flow (`components/ui/save-favorite-sheet.tsx`, `app/(app)/(tabs)/index.tsx`) and proved it with a save-payload test.

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Apply the Crawl Plan Protocol as Form A for Rider Magazine and commit all required artifacts under `.spec/prds/curation-hardening/crawl-plans/rider_mag/`.
MUST: Validate exactly 50 ingested routes before the task can be marked done.
MUST: Preserve editorial description and ranking metadata in the ingestion output and downstream contracts.
MUST: Tag Rider Magazine records as ground-truth inputs for later calibration work without exposing raw implementation flags in rider-facing UI copy.
NEVER: Accept partial ingest as success for Rider Magazine.
NEVER: Collapse editorial copy into generic autogenerated summary text if the source description exists.
STRICTLY: Keep Rider provenance rendering inside the existing detail hierarchy rather than inventing a dedicated editorial screen or modal.

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective:** Build a Rider Magazine ingest path that scrapes and validates the 50 Best Roads source, emits exactly 50 editorially-grounded route records, preserves editorial description and ranking data, and keeps that provenance accessible to the existing mobile detail path.

**Success looks like:** The Rider Magazine source produces exactly 50 validated route records with preserved editorial copy, ground-truth metadata, and rider-facing provenance that survives staging, Convex, and the existing mobile detail surface.

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Problem:** Epic 4 depends on Rider Magazine as its editorial anchor, but the repo has no Rider Magazine source module, no crawl-plan artifacts, and no guaranteed path for preserved editorial provenance into the current route detail UX.

**Why it matters:** Epic 8 calibration quality depends on Rider Magazine being complete and trustworthy. If this source ingests partially or loses editorial context, downstream calibration becomes noisy.

**Current state:** There is no `rider_mag.py` source module. The mobile detail flow exists, but current lean sync/detail contracts do not yet guarantee preservation of all Epic 4 provenance fields.

**Desired state:** Rider Magazine yields a complete, verifiable, provenance-preserving ingest path with no missing routes and no raw implementation flags shown to riders.

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: Rider Magazine crawl-plan artifacts are committed and valid ← PASS: Form A artifact set now includes `site-map.md`, and the crawl report PASS verdict is backed by the full required bundle (evidence: `.spec/prds/curation-hardening/crawl-plans/rider_mag/site-map.md:1`, `.spec/prds/curation-hardening/crawl-plans/rider_mag/crawl-report.md:30`)
  GIVEN: Rider Magazine is a Form A editorial HTML source
  WHEN: crawl-plan inventory, fixtures, selectors, fixture tests, and crawl report are created under `.spec/prds/curation-hardening/crawl-plans/rider_mag/`
  THEN: required fields have full fixture yield and the crawl report records a PASS verdict

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/sources/test_rider_mag_fixtures.py
  TEST_FUNCTION: test_rider_mag_fixtures_cover_required_fields

AC-2: Rider Magazine ingestion emits exactly 50 records with editorial metadata ← PASS: the Rider source still writes exactly 50 routes with serialized editorial provenance and rank metadata (evidence: `scripts/curation/tests/sources/test_rider_mag.py:21`)
  GIVEN: Rider Magazine crawl-plan artifacts and fixtures are valid
  WHEN: `python -m scripts.curation.pipeline.sources.rider_mag` runs
  THEN: `staging/rider_mag.jsonl` contains exactly 50 route records with route name, state, editorial description, source provenance, and source rank metadata

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/sources/test_rider_mag.py
  TEST_FUNCTION: test_rider_mag_ingest_emits_exactly_fifty_records

AC-3: Rider Magazine records carry explicit ground-truth metadata for calibration ← PASS: ground-truth and published-order metadata remain machine-usable in both in-memory and serialized records (evidence: `scripts/curation/tests/sources/test_rider_mag.py:51`)
  GIVEN: Rider Magazine records are ingested into staging
  WHEN: the source records are normalized for scoring and later calibration
  THEN: each record carries the agreed ground-truth provenance fields and source rank metadata in a machine-usable form

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/sources/test_rider_mag.py
  TEST_FUNCTION: test_rider_mag_records_include_ground_truth_metadata

AC-4: Editorial description survives serialization and rider-facing detail rendering ← PASS: the real save flow now carries `routeProvenance` into the saved-route contract and the detail/save contract test proves it (evidence: `app/(app)/(tabs)/index.tsx:989`, `components/ui/save-favorite-sheet.tsx:49`, `app/(app)/saved-route/__tests__/[id].test.ts:373`)
  GIVEN: Rider Magazine records have been pushed through Convex and the detail flow
  WHEN: a rider opens an Epic 4 Rider route in the existing detail surface
  THEN: the editorial description and human-readable Rider provenance appear without exposing raw flags such as `ground_truth=true`

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: app/(app)/saved-route/__tests__/[id].test.ts
  TEST_FUNCTION: test_rider_mag_detail_preserves_editorial_provenance

AC-5: Missing editorial enrichment degrades gracefully ← PASS: partial editorial provenance now omits empty rows cleanly without blank cards or broken layout (evidence: `app/(app)/saved-route/__tests__/[id].test.ts:353`)
  GIVEN: one Rider Magazine record has partial geographic or enrichment data
  WHEN: the existing detail surface renders the route
  THEN: the layout omits empty editorial sections cleanly and falls back to summary/one-liner content without broken spacing or blank cards

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: app/(app)/saved-route/__tests__/[id].test.ts
  TEST_FUNCTION: test_rider_mag_detail_handles_partial_editorial_data

--------------------------------------------------------------------------------
TEST CRITERIA (Boolean Verification)
--------------------------------------------------------------------------------

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Rider Magazine crawl-plan files exist and crawl report verdict is PASS | AC-1 | `test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/urls.jsonl && test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/selectors.yaml && test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/crawl-report.md && rg -n "^\\*\\*?Verdict:\\*\\*? PASS$" .spec/prds/curation-hardening/crawl-plans/rider_mag/crawl-report.md` | [x] TRUE [ ] FALSE |
| 2 | Rider Magazine fixture tests pass locally | AC-1 | `PYTHONPATH=$(pwd) python -m pytest scripts/curation/tests/sources/test_rider_mag_fixtures.py -v` | [x] TRUE [ ] FALSE |
| 3 | Rider Magazine staging output contains exactly 50 records | AC-2 | `PYTHONPATH=$(pwd) python -m scripts.curation.pipeline.sources.rider_mag && python - <<'PY'\nfrom pathlib import Path\np = Path('staging/rider_mag.jsonl')\nassert p.is_file()\nassert len(p.read_text().splitlines()) == 50\nprint('OK')\nPY` | [x] TRUE [ ] FALSE |
| 4 | Rider Magazine records carry machine-usable ground-truth metadata | AC-3 | `PYTHONPATH=$(pwd) python -m pytest scripts/curation/tests/sources/test_rider_mag.py -k ground_truth -v` | [x] TRUE [ ] FALSE |
| 5 | Existing detail tests cover Rider editorial provenance rendering | AC-4 | `pnpm vitest run "app/(app)/saved-route/__tests__/[id].test.ts"` | [x] TRUE [ ] FALSE |
| 6 | Partial editorial data does not break the existing detail layout | AC-5 | `pnpm vitest run "app/(app)/saved-route/__tests__/[id].test.ts"` | [x] TRUE [ ] FALSE |

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. `.spec/prds/curation-hardening/tasks/epic-04-sources-government-editorial/EPIC.md`
   - Lines: ALL
   - Focus: Rider Magazine role in Epic 4 and human-test expectations

2. `.spec/prds/curation-hardening/04-uc-src.md`
   - Lines: ALL
   - Focus: `S4.6` acceptance criteria and source completeness requirements

3. `.spec/prds/curation-hardening/tasks/CRAWL-PLAN-PROTOCOL.md`
   - Lines: ALL
   - Focus: Form A deliverables and PASS requirements

4. `models/curated-routes.ts`
   - Lines: ALL
   - Focus: existing provenance fields and source enum coverage

5. `app/(app)/saved-route/[id].tsx`
   - Lines: ALL
   - Focus: current detail surface that must absorb editorial provenance

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED:
- `scripts/curation/pipeline/sources/rider_mag.py` (NEW) - Rider Magazine ingest entrypoint
- `scripts/curation/tests/sources/test_rider_mag.py` (NEW) - Rider ingest/metadata tests
- `scripts/curation/tests/sources/test_rider_mag_fixtures.py` (NEW) - crawl-plan fixture tests
- `.spec/prds/curation-hardening/crawl-plans/rider_mag/urls.jsonl` (NEW) - Rider inventory
- `.spec/prds/curation-hardening/crawl-plans/rider_mag/selectors.yaml` (NEW) - Rider selectors
- `.spec/prds/curation-hardening/crawl-plans/rider_mag/crawl-report.md` (NEW) - PASS/FAIL report
- `fixtures/rider_mag/` (NEW) - Rider HTML fixtures and manifest
- `scripts/curation/pipeline/sync/convex_push.py` (MODIFY) - only if missing Rider provenance fields
- `models/curated-routes.ts` (MODIFY) - add Rider source/provenance support if missing
- `app/(app)/saved-route/[id].tsx` (MODIFY) - existing detail rendering only

WRITE-PROHIBITED:
- Any new editorial-only mobile screen or modal - out of scope
- Any Epic 8 calibration implementation files - out of scope for this task
- Any unrelated discovery screen refactor - out of scope

MUST:
- [x] Validate the output count is exactly 50
- [x] Preserve editorial description where the source provides it
- [x] Keep rider-facing source language human-readable
- [x] Add explicit tests for partial-data rendering on the existing detail surface

MUST NOT:
- [x] Accept 49/50 or partial ingest as done
- [x] Show raw backend flags in rider-facing copy
- [x] Replace editorial description with placeholder boilerplate when the source text exists
- [x] Add a separate Rider Magazine UI route

--------------------------------------------------------------------------------
DESIGN
--------------------------------------------------------------------------------

**References**

- `.spec/prds/curation-hardening/tasks/epic-04-sources-government-editorial/EPIC.md`
- `.spec/prds/curation-hardening/tasks/CRAWL-PLAN-PROTOCOL.md`
- `models/curated-routes.ts`
- `app/(app)/saved-route/[id].tsx`
- `components/sheets/route-details-sheet.tsx`

**Pattern:** Treat Rider Magazine as compact editorial provenance added to the existing detail hierarchy, with the ingest side validated through the same crawl-plan discipline used for other high-value HTML sources.

**Anti-pattern:** Do not create a special editorial presentation layer just because the source is human-curated. Preserve the copy, but keep the UX additive and compact.

--------------------------------------------------------------------------------
AGENT INSTRUCTIONS
--------------------------------------------------------------------------------

AGENT: python-implement

1. Build the Rider crawl-plan artifacts and fixtures first.
2. Implement the Rider source with exact-count validation.
3. Preserve editorial description and rank metadata during normalization.
4. Ensure machine-usable ground-truth provenance exists for later calibration.
5. Add rider-facing rendering/tests only on the existing detail surface.

--------------------------------------------------------------------------------
ORCHESTRATOR VERIFICATION PROTOCOL
--------------------------------------------------------------------------------

AFTER RED PHASE:
  RUN: `PYTHONPATH=$(pwd) python -m pytest scripts/curation/tests/sources/test_rider_mag.py -v`
  EXPECT: new tests fail for the missing Rider functionality

AFTER GREEN PHASE:
  RUN: `PYTHONPATH=$(pwd) python -m pytest scripts/curation/tests/sources/test_rider_mag.py scripts/curation/tests/sources/test_rider_mag_fixtures.py -v`
  EXPECT: Rider source and fixture tests pass

AFTER DETAIL/UI WORK:
  RUN: `pnpm vitest run "app/(app)/saved-route/__tests__/[id].test.ts"`
  EXPECT: editorial provenance tests pass

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent:** `python-implement`  
**Rationale:** The core work is HTML source ingestion, validation, and metadata preservation.

**Review Agent:** `python-review`  
**Rationale:** This task needs review for completeness, metadata integrity, and partial-data handling.

--------------------------------------------------------------------------------
EVIDENCE GATES
--------------------------------------------------------------------------------

Gate 1: Rider crawl-plan artifacts complete  
Command: `test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/urls.jsonl && test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/selectors.yaml && test -f .spec/prds/curation-hardening/crawl-plans/rider_mag/crawl-report.md`  
Expected: exit 0

Gate 2: Rider source tests pass  
Command: `PYTHONPATH=$(pwd) python -m pytest scripts/curation/tests/sources/test_rider_mag.py scripts/curation/tests/sources/test_rider_mag_fixtures.py -v`  
Expected: exit 0

Gate 3: Rider staging count is exactly 50  
Command: `PYTHONPATH=$(pwd) python -m scripts.curation.pipeline.sources.rider_mag`  
Expected: `staging/rider_mag.jsonl` written with 50 lines

Gate 4: Existing detail rendering handles editorial provenance  
Command: `pnpm vitest run "app/(app)/saved-route/__tests__/[id].test.ts"`  
Expected: exit 0

--------------------------------------------------------------------------------
REVIEW NOTES
--------------------------------------------------------------------------------

- 2026-04-16 review: NEEDS_FIXES
- Finding 1: Crawl Plan Protocol Form A artifacts are incomplete. `crawl-plans/rider_mag/` is missing the required Phase 0 `site-map.md` even though the crawl report marks the source PASS. See `tasks/CRAWL-PLAN-PROTOCOL.md` Phase 0 deliverable and conventional path requirements.
- Finding 2: Rider provenance is only verified through mocked `routeProvenance` detail tests. The common save flow in `components/ui/save-favorite-sheet.tsx` still calls `saveRoute` without a `routeProvenance` payload, so this task does not yet prove provenance survives the real save-to-detail path.

--------------------------------------------------------------------------------
REVIEW CRITERIA
--------------------------------------------------------------------------------

- [x] Crawl-plan deliverables are honest and complete
- [x] Exactly 50 Rider routes are emitted
- [x] Editorial description and rank metadata survive normalization
- [x] Rider-facing provenance avoids raw implementation flags
- [x] Partial-data rendering does not break the existing detail layout

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

- `BASE-009b`
- `INF-001`
- `INF-002`

--------------------------------------------------------------------------------
NOTES
--------------------------------------------------------------------------------

- Keep the machine-readable ground-truth flagging in data contracts even if the UI never renders those raw field names.
- If location resolution for one or more Rider routes is incomplete, fail loudly in logs/tests rather than silently under-counting the source.
