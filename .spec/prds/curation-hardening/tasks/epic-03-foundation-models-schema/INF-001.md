================================================================================
TASK: INF-001 - Install New Python Dependencies
================================================================================

TASK_TYPE: INFRA
STATUS: Backlog
PRIORITY: P0
EFFORT: XS
ESTIMATE: 30 minutes
AGENT: python-implement
ITERATION: 1

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Problem:** The curation-hardening pipeline needs 6 additional Python libraries for fuzzy matching (rapidfuzz), geospatial data reading (fiona), Reddit API access (praw), elevation data (srtm.py), GPX parsing (gpxpy), and distance calculations (haversine).

**Why it matters:** Every subsequent source, scoring, and quality task depends on these libraries being available. Without them, deduplication (rapidfuzz), Scenic Byways GIS ingestion (fiona), Reddit ingestion (praw), and elevation scoring (srtm.py, gpxpy, haversine) cannot proceed.

**Current state:** `scripts/curation/requirements.txt` has requests, python-dotenv, httpx, beautifulsoup4, playwright, anthropic, instructor, pydantic, scipy, shapely, pyproj. Missing: rapidfuzz, fiona, praw, srtm.py, gpxpy, haversine.

**Desired state:** All 6 missing dependencies added to `scripts/curation/requirements.txt` and importable without conflicts.

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Add ALL 6 missing dependencies to scripts/curation/requirements.txt
MUST: Verify each dependency installs without conflict using pip install
MUST: Maintain existing dependencies — no version removals or downgrades
NEVER: Modify project root requirements.txt if it exists
NEVER: Pin specific versions (==) unless required for compatibility — use >= for flexibility
STRICTLY: File path is scripts/curation/requirements.txt (NOT project root)

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective:** Install required Python dependencies for fuzzy matching, geospatial operations, Reddit API, and elevation data processing.

**Success state:** All 6 missing dependencies (rapidfuzz, fiona, praw, srtm.py, gpxpy, haversine) are added to scripts/curation/requirements.txt and install successfully without breaking existing pipeline imports.

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: Install all missing dependencies successfully
  GIVEN: A fresh Python virtual environment
  WHEN: I run `pip install -r scripts/curation/requirements.txt`
  THEN: All packages install without errors and all existing pipeline modules still import successfully

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/test_inf001_dependencies.py
  TEST_FUNCTION: test_all_dependencies_installable

AC-2: Verify rapidfuzz imports for fuzzy matching
  GIVEN: rapidfuzz is installed in requirements.txt
  WHEN: I import rapidfuzz in a Python REPL
  THEN: The import succeeds and fuzz.ratio() function is callable

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/test_inf001_dependencies.py
  TEST_FUNCTION: test_rapidfuzz_import

AC-3: Verify fiona and shapely for geospatial operations
  GIVEN: fiona and shapely are installed
  WHEN: I import both modules in a Python REPL
  THEN: Both imports succeed and basic geometry operations work

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/test_inf001_dependencies.py
  TEST_FUNCTION: test_fiona_shapely_import

AC-4: Handle GDAL dependency conflict for fiona
  GIVEN: fiona requires GDAL system library
  WHEN: pip install fails due to missing GDAL
  THEN: Document the GDAL installation requirement in a HANDOFF.md comment and surface to user for resolution

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: scripts/curation/tests/test_inf001_dependencies.py
  TEST_FUNCTION: test_no_duplicate_dependencies

Quality Criteria:
- [ ] All imports succeed (one test per AC minimum)
- [ ] No dependency conflicts
- [ ] Existing pipeline modules still import

--------------------------------------------------------------------------------
TEST CRITERIA (Boolean Verification)
--------------------------------------------------------------------------------

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | All 6 dependencies are present in scripts/curation/requirements.txt | AC-1 | `grep -E '^(rapidfuzz|fiona|praw|srtm.py|gpxpy|haversine)' scripts/curation/requirements.txt \| wc -l` | [ ] TRUE  [ ] FALSE |
| 2 | pip install completes with exit code 0 | AC-1 | `pip install -r scripts/curation/requirements.txt --dry-run` | [ ] TRUE  [ ] FALSE |
| 3 | Existing pipeline modules import without errors | AC-1 | `python -c 'from scripts.curation.pipeline.models import Route, EnrichedRoute; from scripts.curation.pipeline.scoring.composite import WEIGHTS; print("OK")'` | [ ] TRUE  [ ] FALSE |
| 4 | No duplicate dependency entries exist in requirements.txt | AC-4 | `sort scripts/curation/requirements.txt \| uniq -d \| wc -l` | [ ] TRUE  [ ] FALSE |

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. scripts/curation/requirements.txt
   - Lines: ALL
   - Focus: Current dependency list and formatting pattern

2. scripts/curation/pipeline/models.py
   - Lines: 1-50
   - Focus: Current imports to ensure no conflicts with new packages

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED:
- scripts/curation/requirements.txt (MODIFY - add 6 new dependencies)

WRITE-PROHIBITED:
- requirements.txt (project root) - do NOT modify
- setup.py, pyproject.toml - do NOT create or modify
- Any .lock files - do NOT create

MUST:
- [ ] Follow existing formatting pattern from requirements.txt
- [ ] Add dependencies in alphabetical order

MUST NOT:
- [ ] Remove any existing dependencies
- [ ] Pin versions with == unless compatibility requires it
- [ ] Modify any other files

--------------------------------------------------------------------------------
CODE PATTERN (Reference)
--------------------------------------------------------------------------------

Dependency addition pattern (alphabetical within file):
```
# Existing: anthropic, beautifulsoup4, httpx, instructor, playwright, pydantic, pyproj, python-dotenv, requests, scipy, shapely

# New (add in alphabetical order):
fiona>=X.Y
gpxpy>=X.Y
haversine>=X.Y
praw>=X.Y
rapidfuzz>=X.Y
srtm.py>=X.Y
```

Anti-pattern: Do NOT pin `==` versions unless a known conflict exists.

--------------------------------------------------------------------------------
AGENT INSTRUCTIONS
--------------------------------------------------------------------------------

AGENT: python-implement

## IMPLEMENTATION STEPS:

1. Read scripts/curation/requirements.txt to confirm current state
2. Add rapidfuzz (fuzzy string matching for road name deduplication)
3. Add fiona (geospatial data format support, complements shapely)
4. Add praw (Reddit API wrapper for social source extraction)
5. Add srtm.py (SRTM elevation data fetching)
6. Add gpxpy (GPX file parsing for route uploads)
7. Add haversine (distance calculations for route validation)
8. Run pip install -r scripts/curation/requirements.txt to verify no conflicts
9. Surface any GDAL/system dependency issues to user with installation instructions
10. Test imports for all 6 new dependencies individually

## VERIFICATION CHECKLIST:
- [ ] All 6 dependencies added to scripts/curation/requirements.txt
- [ ] pip install -r scripts/curation/requirements.txt completes successfully
- [ ] rapidfuzz.fuzz.ratio() callable in Python REPL
- [ ] fiona and shapely import without GDAL errors (or GDAL requirement documented)
- [ ] praw.Reddit class instantiates without errors
- [ ] srtm.py get_data() function is available
- [ ] gpxpy.parse() function is available
- [ ] haversine.haversine() function is available
- [ ] Existing pipeline modules (models, scoring, extraction) still import
- [ ] No duplicate dependencies in requirements.txt

--------------------------------------------------------------------------------
ORCHESTRATOR VERIFICATION PROTOCOL
--------------------------------------------------------------------------------

AFTER IMPLEMENTATION:
  RUN: python -c 'import rapidfuzz, fiona, praw, srtm, gpxpy, haversine; print("All imports OK")'
  EXPECT: Prints 'All imports OK' without exception

  RUN: python -c 'from scripts.curation.pipeline.models import Route; print("Pipeline imports OK")'
  EXPECT: Prints 'Pipeline imports OK' without ModuleNotFoundError

  RUN: pip install -r scripts/curation/requirements.txt --dry-run
  EXPECT: Exit code 0, no dependency conflicts

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent:** python-implement
**Rationale:** Python dependency installation is a core Python infrastructure task within the pipeline codebase

**Review Agent:** convex-reviewer (if schema-impacting) or manual review
**Rationale:** Dependency additions are low-risk; verify no import conflicts with existing code

--------------------------------------------------------------------------------
EVIDENCE GATES
--------------------------------------------------------------------------------

Gate 1: Dependency installation succeeds
  Command: `pip install -r scripts/curation/requirements.txt --dry-run`
  Expected: exit code 0, no dependency conflicts reported

Gate 2: New modules import successfully
  Command: `python -c 'import rapidfuzz, fiona, praw, srtm, gpxpy, haversine; print("All imports OK")'`
  Expected: Prints 'All imports OK' without exception

Gate 3: Existing pipeline imports still work
  Command: `python -c 'from scripts.curation.pipeline.models import Route; print("Pipeline imports OK")'`
  Expected: Prints 'Pipeline imports OK' without ModuleNotFoundError

Gate 4: Type Check (if applicable)
  Command: `npx tsc --noEmit`
  Expected: Exit 0

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- VAL-* (Epic 1 validation tasks complete)
- BASE-001 (Epic 2 baseline pipeline running)

Blocks:
- INF-002 (needs rapidfuzz, shapely installed)
- All SRC/QUAL/SCO/RID tasks in Epic 4+

--------------------------------------------------------------------------------
NOTES
--------------------------------------------------------------------------------

- fiona depends on GDAL system library — may require `brew install gdal` on macOS
- rapidfuzz is MIT-licensed and faster than fuzzywuzzy for name matching
- praw requires Reddit API credentials — add to HANDOFF.md if not already present
- shapely is already installed; fiona complements it for reading Shapefile/GeoJSON formats

================================================================================
