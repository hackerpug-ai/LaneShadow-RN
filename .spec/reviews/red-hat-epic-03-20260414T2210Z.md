# Red-Hat Review Report — Epic 3 Foundation Models & Schema

**Report Date**: 2026-04-14T22:10:47Z
**Target**: Epic 3 — Foundation / Semantic Matching Infrastructure
**Path**: `.spec/prds/curation-hardening/tasks/epic-03-foundation-models-schema`
**Reviewed By**: python-review, convex-reviewer, code-reviewer (3 agents)
**Backend-reviewer**: dispatch failed (agent type not found in roster)

## Executive Summary

**Verdict: NEEDS_FIXES. Epic 3 is ~75% implemented but blocked by critical stubs, missing tests, and lying status markers.**

Contrary to the "STATUS: Backlog" markers on every INF task file, the majority of Epic 3 code HAS landed — requirements.txt, Python dataclasses, Convex schema with vectorIndex, semanticSearch.ts, convex_push.py, and PostExtraction v2 schema all exist and largely match their acceptance criteria. However, INF-004 contains an explicit `NotImplementedError` in the critical path (`load_routes_needing_embedding`), two of INF-004's most important CLI tests are `@pytest.mark.skip`-ed test theatre, INF-002 and INF-006 have zero test coverage, and **every single TDD_STATE checkbox across all 7 task files is unchecked**. On top of that, the plan approved earlier today (`validated-whistling-journal.md`) with waypoint-ready data shape additions has 0% implementation.

**Answer to "are we done": No. Three CRITICAL blockers + one full deferred plan.**

## Discrepancy Between Agents (Addressed)

**python-review** reported "nothing implemented" based on a narrow `git log` scan of the task directory. This was **wrong** — the implementation lives in separate commits outside the task dir (`a52d4b8 inf-004`, `420e55d INF-005`, `1cd0a80 inf-006`, etc.). Orchestrator-verified ground truth overrides python-review's git-only verdict. **python-review's AC table is discarded** in favor of file-based evidence from code-reviewer and convex-reviewer.

## AC Verdict Table (Ground Truth — Verified)

### INF-001 — Install Dependencies
| AC | Verdict | Evidence |
|----|---------|----------|
| openai==1.54.3, anthropic==0.39.0 pinned | ✅ PASS | `scripts/curation/requirements.txt:17,9` |
| 5 geometry/source libs pinned | ✅ PASS | `requirements.txt:18-22` (fiona, praw, srtm.py, gpxpy, haversine) |
| rapidfuzz absent | ✅ PASS | `grep -c rapidfuzz = 0` |
| Fresh virtualenv install verified | ⚠️ PARTIAL | No install artifact committed |
| All new packages importable | ⚠️ PARTIAL | No import verification recorded |
| TDD_STATE | ❌ FAIL | 0/5 checkboxes marked |

### INF-002 — Extended Route Models
| AC | Verdict | Evidence |
|----|---------|----------|
| Route semantic matching fields | ✅ PASS | `models.py:12` Route class with new fields |
| EnrichedRoute 9 scoring fields | ✅ PASS | `models.py:69` |
| LLMExtractionArtifact dataclass | ✅ PASS | `models.py:105` |
| RouteMatch dataclass | ✅ PASS | `models.py:121` |
| PostExtraction importable from models | ⚠️ PARTIAL | Wrapped in try/except with silent `None` fallback (semantic stub) |
| Backward compatibility maintained | ⚠️ PARTIAL | Original 12-field signature changed — `length_miles`, bounds fields became Optional defaults |
| **test_inf002_models.py exists** | ❌ **FAIL** | **File absent** — INF-002 has zero automated coverage |
| TDD_STATE | ❌ FAIL | 0/6 checkboxes marked |

### INF-003 — Convex Vector Index + Match Audit Schema
| AC | Verdict | Evidence |
|----|---------|----------|
| curatedRouteValidator new optional fields | ✅ PASS | `models/curated-routes.ts:149-193` |
| vectorIndex("by_embedding", dims: 1536) | ✅ PASS | `convex/schema.ts:178-182` |
| route_posts_raw table + 3 indexes | ✅ PASS | `schema.ts:208-211` |
| route_matches table + 3 indexes | ✅ PASS | `schema.ts:219-222` |
| Non-breaking (all optional) | ✅ PASS | Verified in validator |
| No legacy route_mentions table | ✅ PASS | Not present |
| schema.inf003.test.ts exists | ✅ PASS | `convex/__tests__/schema.inf003.test.ts` |
| TDD_STATE | ❌ FAIL | 0/6 checkboxes marked |

### INF-004 — Route Embedding Generation Pipeline
| AC | Verdict | Evidence |
|----|---------|----------|
| batch_embed_routes.py exists | ✅ PASS | File at `scripts/curation/pipeline/embed/batch_embed_routes.py` |
| build_search_text, generate_embeddings, batch_embed_routes | ✅ PASS | Functions present |
| EmbeddingCostLedger dataclass | ✅ PASS | Present |
| Retry with exponential backoff | ✅ PASS | Lines 187-210 |
| Routes with empty search_text skipped | ✅ PASS | Lines 262-268 |
| **CLI --dry-run does NOT push** | ❌ **FAIL** | `test_inf004_embed.py:314` — `@pytest.mark.skip(reason="Complex import mocking - CLI behavior tested manually")` — **test theatre** |
| **CLI --commit pushes to Convex** | ❌ **FAIL** | `test_inf004_embed.py:331` — identical skip pattern — **test theatre** |
| **load_routes_needing_embedding incremental** | ❌ **FAIL (CRITICAL)** | `batch_embed_routes.py:333` — `raise NotImplementedError("load_routes_needing_embedding is not yet implemented. Convex query helper needs to be wired.")` + caught at line 463 — **CLI cannot run end-to-end** |
| TDD_STATE | ❌ FAIL | 0/8 checkboxes marked |

### INF-005 — PostExtraction Schema v2
| AC | Verdict | Evidence |
|----|---------|----------|
| EXTRACTION_SCHEMA_VERSION == 2 | ✅ PASS | `extraction/schema.py:43` |
| PostExtraction BaseModel with all fields | ✅ PASS | `schema.py:142-216` |
| sentiment Literal validation | ✅ PASS | `schema.py:173` |
| JSON round-trip | ✅ PASS | `test_inf005_extraction_schema.py:151-187` |
| RouteAttributes v1 preserved | ✅ PASS | `schema.py:54-134` |
| CACHE_POLICY constant | ✅ PASS | `schema.py:47-51` |
| extraction_confidence bounds | ✅ PASS | `schema.py:195-199` |
| PostExtraction re-exportable | ⚠️ PARTIAL | Silent `None` fallback in models.py try/except |
| TDD_STATE | ❌ FAIL | 0/8 checkboxes marked |

### INF-006 — Convex Vector Search Query Wrappers
| AC | Verdict | Evidence |
|----|---------|----------|
| semanticSearch.ts exists | ✅ PASS | `convex/semanticSearch.ts` |
| 7 exported functions | ✅ PASS | findCandidate, findByIdentifier, updateEmbedding, addRouteMatch, getForPost, getForRoute, getRawPostsForRoute |
| ctx.vectorSearch usage | ✅ PASS | Line 76 |
| Dimension check (1536) | ✅ PASS | Lines 64-67, 232-235 |
| Range validation on addRouteMatch | ✅ PASS | Lines 298-305 |
| .withIndex on route_matches queries | ✅ PASS | Lines 358, 418, 493 |
| geospatialIndex.ts unchanged | ✅ PASS | git diff empty |
| **findRoutesByIdentifier indexed** | ⚠️ PARTIAL | Line 142 uses `ctx.db.query(...).collect()` — full table scan (acknowledged "MVP" in task file, but approved plan requires real indexes) |
| **convex/__tests__/semanticSearch.test.ts exists** | ❌ **FAIL** | **File absent** — INF-006 has zero test coverage |
| TDD_STATE | ❌ FAIL | 0/10 checkboxes marked |

### INF-007 — Convex Push Serialization
| AC | Verdict | Evidence |
|----|---------|----------|
| New field serialization | ✅ PASS | `convex_push.py:88-111` |
| DEFAULT_BATCH_SIZE = 10 | ✅ PASS | Line 23 |
| camelCase field mapping | ✅ PASS | All fields |
| None values omitted | ✅ PASS | `_maybe_set` helper |
| test_inf007_convex_push.py exists | ✅ PASS | But uses `@pytest.mark.skipif(not IMPORTS_AVAILABLE, reason="RED phase")` — 5 skips remain (now post-RED, should be unconditional) |
| Backward compat with old Route | ⚠️ PARTIAL | Silent field omission when composite_score missing |
| TDD_STATE | ❌ FAIL | 0 checkboxes marked |

### INF-011 — US_STATES Allowlist
| AC | Verdict | Evidence |
|----|---------|----------|
| Phase 1: allowlist + cascade | ✅ PASS | Commit `8ca42c5` |
| Phase 2+: full implementation | ⚠️ PARTIAL | Stub file present; P2 priority, non-blocking |

## HIGH Confidence Findings (2+ Agents Agree)

### 🔴 CRITICAL — Incremental mode is a NotImplementedError stub
- **Location**: `scripts/curation/pipeline/embed/batch_embed_routes.py:333`
- **Evidence**: `raise NotImplementedError("load_routes_needing_embedding is not yet implemented. Convex query helper needs to be wired.")`
- **Impact**: The CLI's `--commit` and `--incremental` paths cannot run end-to-end. The embedding backfill — the whole point of INF-004 — is non-functional.
- **Agents**: code-reviewer (CRITICAL), convex-reviewer (dependency flag)
- **Fix**: Wire `load_routes_needing_embedding` to an existing Convex fetch helper OR add a new thin HTTP query. Treat as a blocker for any Epic 6/9/10 work.

### 🔴 CRITICAL — Test theatre on CLI behavior (AC-6 and AC-7)
- **Location**: `scripts/curation/tests/test_inf004_embed.py:314`, `:331`
- **Evidence**: `@pytest.mark.skip(reason="Complex import mocking - CLI behavior tested manually")` with `pass` body
- **Impact**: The two most important behavioral guarantees of the CLI — that `--dry-run` does NOT push and `--commit` DOES push — have zero automated enforcement. A regression can ship silently.
- **Agents**: code-reviewer (HIGH), convex-reviewer (dependency)
- **Fix**: Replace skips with `unittest.mock.patch("scripts.curation.pipeline.sync.convex_push.push_routes")` — straightforward pattern, no infra needed.

### 🔴 HIGH — Status markers lie about progress
- **Location**: All 7 INF-00x.md files
- **Evidence**: Every task file has `STATUS: Backlog` in the frontmatter despite 75% of the code being merged (see commits `a52d4b8`, `420e55d`, `1cd0a80`, `d00e508`, `e30d1a1`)
- **Impact**: Impossible to tell at a glance what is done, partial, or not started. Downstream planning (Epic 6/9/10) cannot trust the task list. TDD_STATE checkboxes (~50 total) are all unchecked.
- **Agents**: code-reviewer, convex-reviewer, python-review (all 3)
- **Fix**: Update task files in place — mark PASS items checked, annotate FAIL/PARTIAL inline with evidence, update STATUS to `In Review` or `Done` per item.

### 🟡 HIGH — Missing test files for INF-002 and INF-006
- **Location**: Absent — `scripts/curation/tests/test_inf002_models.py`, `convex/__tests__/semanticSearch.test.ts`
- **Impact**: Two tasks (INF-002 models, INF-006 semantic search) have zero automated coverage. Any refactor or migration will silently break these.
- **Agents**: code-reviewer, convex-reviewer
- **Fix**: Create both test files using the AC tables in their task specs as the source of truth.

### 🟡 HIGH — PostExtraction re-export uses silent None fallback
- **Location**: `scripts/curation/pipeline/models.py:139-144`
- **Evidence**: `try: from scripts.curation.pipeline.extraction.schema import PostExtraction; except ImportError: PostExtraction = None`
- **Impact**: Callers that import `PostExtraction` from models get `None` with no error if the import path is broken — a latent time bomb. Downstream `isinstance(x, PostExtraction)` or type annotations fail at runtime, not import time.
- **Agents**: code-reviewer
- **Fix**: Remove try/except; INF-005 has landed, the import is unconditional.

### 🟡 HIGH — TypeScript `type = any` erasure in semanticSearch.ts (partially fixed)
- **Location**: `convex/semanticSearch.ts:26-28` — but see commit `e30d1a1 fix: type vectorSearch results`
- **Evidence**: Recent commit added real types for vectorSearch results; convex-reviewer noted `type CuratedRouteDoc = any` was originally present. Post-fix state may be correct; needs spot-check.
- **Agents**: convex-reviewer, code-reviewer
- **Fix**: Verify `_generated/dataModel` types are used. If `any` remains anywhere, replace with `Doc<"curated_routes">`.

## MEDIUM Findings

### 🟡 MEDIUM — findRoutesByIdentifier is a full table scan
- **Location**: `convex/semanticSearch.ts:142`
- **Evidence**: `ctx.db.query("curated_routes").collect()` — no index usage
- **Impact**: At 5k routes with 1536-dim embeddings, each scan deserializes ~30MB of data. Scales badly. Task file acknowledges "MVP acceptable" but approved plan (`validated-whistling-journal.md`) requires real indexes.
- **Fix**: Add `by_name_lower`, `by_highway_number` secondary indexes; refactor query to `.withIndex()`.

### 🟡 MEDIUM — INF-007 tests still gated on RED-phase imports
- **Location**: `scripts/curation/tests/sync/test_convex_push.py:40,84,125,185,215`
- **Evidence**: 5 tests use `@pytest.mark.skipif(not IMPORTS_AVAILABLE, reason="Module not created yet - RED phase")`
- **Impact**: Test is in "pending" mode post-implementation. If the import condition fails silently, all 5 tests are no-ops.
- **Fix**: Remove the skipif now that convex_push.py is implemented; make imports unconditional.

### 🟡 MEDIUM — INF-002 signature change risks hidden backward-compat break
- **Location**: `scripts/curation/pipeline/models.py:25`
- **Evidence**: `length_miles`, `bounds_ne_*`, `bounds_sw_*` became `Optional[float] = None`. `composite_score`/`curvature_score` may have moved to `EnrichedRoute`.
- **Impact**: Older code instantiating `Route(*12_positional_args)` breaks without a clear error. The backward-compat AC (AC-6 in INF-002) is not verified by any test.
- **Fix**: Write the missing `test_inf002_models.py` with AC-6's exact instantiation pattern.

## Plan Delta — Approved Adjustment Not Yet Applied

The plan approved at 2026-04-14T~21:00Z (`validated-whistling-journal.md`) has **0% implementation**. None of these landed:

| Item | Location | Status |
|------|----------|--------|
| `descriptiveSummary` on curated_routes | `convex/models/curated-routes.ts` | ❌ Absent |
| `postEmbedding` on route_posts_raw | `convex/models/curated-routes.ts` | ❌ Absent |
| `community_waypoint_mentions` table | `convex/schema.ts` | ❌ Absent |
| PostExtraction v3 (`waypoint_mentions`) | `extraction/schema.py` | ❌ Still v2 |
| `findCandidateRoutesHybrid` entry point | `convex/semanticSearch.ts` | ❌ Absent |
| `addCommunityWaypointMention` mutation | `convex/semanticSearch.ts` | ❌ Absent |
| Indexed `findRoutesByIdentifier` | `convex/semanticSearch.ts` | ❌ Still scan |
| INF-012 task file (descriptive summary pre-gen) | task dir | ❌ Absent |
| INF-013 task file (calibration) | task dir | ❌ Absent |
| Waypoints PRD edits (`candidate_route_ids`) | `.spec/prds/waypoints/09-technical-requirements.md` | ❌ Untouched |

## Stub Findings Summary (Severity Ranked)

| # | Severity | Type | Location | Fix complexity |
|---|----------|------|----------|----------------|
| 1 | CRITICAL | Explicit Stub | `batch_embed_routes.py:333` (NotImplementedError) | M — wire to Convex fetch |
| 2 | CRITICAL | Test Theatre | `test_inf004_embed.py:314,331` (skip+pass) | S — mock.patch |
| 3 | HIGH | Semantic Stub | `models.py:139-144` (PostExtraction `None` fallback) | XS — unconditional import |
| 4 | HIGH | Missing Tests | `test_inf002_models.py` absent | M — write from AC table |
| 5 | HIGH | Missing Tests | `convex/__tests__/semanticSearch.test.ts` absent | M — write from AC table |
| 6 | HIGH | Status Drift | All INF-0xx.md files STATUS: Backlog | S — in-place edit |
| 7 | MEDIUM | Performance Stub | `semanticSearch.ts:142` (full scan) | M — add indexes |
| 8 | MEDIUM | Lingering Skips | `test_convex_push.py` 5 RED-phase skipif | XS — remove conditional |

## TDD State Summary

Across all 7 INF tasks (~50 TDD_STATE entries total):

| State | Count Checked |
|-------|---------------|
| RED | 0 |
| VERIFY_RED | 0 |
| GREEN | 0 |
| VERIFY_GREEN | 0 |
| REFACTOR | 0 |

**Zero TDD cycles recorded as complete.** Either the TDD protocol was skipped entirely during implementation, or the task files were never updated. Either way, the TDD discipline Epic 3 requires is not enforced.

## Recommendations (Priority Ordered)

1. **Unblock INF-004**: implement `load_routes_needing_embedding` against an existing Convex fetch helper. Without it, nothing downstream (Epic 6/9/10) can run the pipeline end-to-end. (~60 min)
2. **Replace test theatre** in `test_inf004_embed.py:314,331` with real `unittest.mock.patch` coverage for --dry-run/--commit. (~30 min)
3. **Remove PostExtraction silent fallback** in `models.py:139-144`. Unconditional import. (~5 min)
4. **Write missing tests**: `test_inf002_models.py` and `convex/__tests__/semanticSearch.test.ts` from the AC tables in the task files. (~2h each)
5. **Update INF-00x.md status markers** to reflect reality: check off PASS items, annotate FAIL/PARTIAL with evidence, set STATUS accurately. (~1h)
6. **Decide on plan delta**: Either (a) start executing the approved `validated-whistling-journal.md` plan (waypoint-ready data shape + INF-012/013), or (b) explicitly defer with a note in EPIC.md. Leaving it in limbo is the worst option. (USER DECISION)
7. **Remove RED-phase skipif** in `test_convex_push.py` (~10 min)
8. **Index `findRoutesByIdentifier`** before Epic 9/10 runs against it. (~30 min)

## Metadata

- **Agents dispatched**: 4 (python-review, convex-reviewer, code-reviewer, backend-reviewer)
- **Agents reporting**: 3 — backend-reviewer failed (agent type not in roster; not a blocker given code-reviewer's coverage)
- **Confidence framework**: HIGH (2+ agents agree), MEDIUM (1 agent + orchestrator verification), LOW (1 agent only)
- **Python-review discarded**: relied on narrow git log, missed implementation commits outside task dir — orchestrator overrode verdict
- **Report duration**: ~3 minutes
- **Next steps**: Remediate CRITICAL stubs before any Epic 6/9/10 work; decide on plan delta execution
