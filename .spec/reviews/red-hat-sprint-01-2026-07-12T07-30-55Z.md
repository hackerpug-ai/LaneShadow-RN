# Red-Hat Review Report

**Report Date**: 2026-07-12T07:30:55Z  
**Target**: Sprint 01 — Geometry reference-flow spike  
(`.spec/prds/route-agent-quality/tasks/sprint-01-geometry-reference-spike`)  
**Branch / HEAD**: `main` @ `44e7b2eaf90bf23fd444257572b2fdf5c0d03c77`  
**Reviewed By**: aisdk-reviewer, convex-reviewer, react-native-ui-reviewer + orchestrator gate-pre-check  
**Mandate**: Report only — no fixes implemented. **Do not treat this sprint as done.**

---

## Executive Summary

Sprint 01 ships a **largely real** reconstruct→gate→persist→riderReady→query→render seam: direct AI-SDK v7 anchor extraction (S1-T1), a pure VER-01 gate + Convex reconstruct path with side-table geometry (S1-T2), and a cold-boot Maestro flow with a ≥2-point real-line probe (S1-T3). **However, the sprint’s “Done” claim and `gate-results.json` verdict `"pass"` do not survive adversarial re-review.**

Three blocking themes:

1. **Human Testing Gate evidence theatre** — steps 1–3 re-read already-persisted verification (same `cmd_sha`) instead of running reconstruct; steps 6–7 substitute Maestro for founder eyes; **step7.png shows a blank grey map with no road line**.
2. **S1-T3 visual oracle gap** — Maestro GREEN on an invisible 1px `curated-route-detail-real-line` probe while the Mapbox viewport does not paint a copper line (probe ≠ plotted line).
3. **S1-T2 test soft-asserts + unauthenticated public reconstruct surface** — AC-4/5/6 integration tests can vacuous-pass; public wrappers have no `requireIdentity`.

**Overall disposition: NEEDS_FIXES. Sprint is NOT complete. Do not unblock Sprint 04 on this gate package alone.**

---

## HIGH Confidence Findings (3+ Agents Agree / Orchestrator + Multi-Reviewer)

- [ ] **Finding H1 — Gate step 7 visual claim FAILS seeded evidence** | Severity: **Critical**  
      Agents: react-native-ui-reviewer, orchestrator (image review of `/tmp/.../step7.png`), gate-pre-check  
      **Evidence**: Human gate step 7 requires “Watch the correct road line plot on the map.” Captured `step7.png` shows Route Detail for *Twist of Tepusquet Loop* with a **blank grey map**, a blue location puck only, and **no copper/road polyline**. Maestro still reported `curated-route-detail-real-line` COMPLETED.  
      **Implication**: `gate-results.json` `verdict: "pass"` + `gate-verification.json` `recomputed_verdict: "pass"` overclaim the human-visible outcome.

- [ ] **Finding H2 — real-line testID is probe theatre vs “road line plots”** | Severity: **Critical** (for human-visible plot / T-REC-016)  
      Agents: react-native-ui-reviewer, orchestrator  
      **Evidence**: `app/(app)/curated-route/[id].tsx` gates `hasRealRoadLine` on `coordinates.length >= 2` (good vs string-presence) but renders a transparent 1px View (`polylineProbe` styles: `height: 1`, `backgroundColor: 'transparent'`). Maestro asserts that testID, not Mapbox paint / pixel / visual settle.  
      **Implication**: A centroid-dot false positive is prevented, but a **blank map with valid coords still passes** AC-1/TC-1 automation.

- [ ] **Finding H3 — Human gate steps 1–3 did not reconstruct** | Severity: **High**  
      Agents: orchestrator (gate-pre-check), convex-reviewer (executability vs actual logs)  
      **Evidence**: Steps 1, 2, and 3 all share `cmd_sha=c565645c…` and run:
      ```text
      npx convex run curatedGeometryReconstruct:getVerificationForRoute
        {"routeId":"motorcycleroads:twist-of-tepusquet-loop"}
      ```
      Documented step 1 is **reconstruct** via `reconstructForRoute` on real APIs. Actual run is a **read of pre-existing** verification (ratio 1.18, provenance `ai_reconstructed`, riderReady true). Persist/recompute is not re-exercised in the gate package.  
      **Implication**: Gate package proves residual state, not a live reconstruct→gate→persist chain at gate time.

- [ ] **Finding H4 — Founder-Operator gate substituted by Maestro; S1-T4 overclaims** | Severity: **High**  
      Agents: react-native-ui-reviewer, orchestrator  
      **Evidence**: SPRINT + S1-T4 require Founder-Operator **watch**. S1-T4 STATUS: `Done (CLI 1-5 + Maestro 6-7)`. `gate-results.json` steps 6–7 are `type: "terminal"`, evidence `"maestro CLI cold-boot flow log"`, `ui_driver: "none"`. Step 6 wording (“tap the recovered route”) vs Maestro **deep-link only** (`openLink`).  
      **Implication**: Human testing gate is not human-verified; automation that fails visual claim still stamped green.

- [ ] **Finding H5 — Unauthenticated public reconstruct / verification surface** | Severity: **High**  
      Agents: convex-reviewer (+ security implication)  
      **Evidence**: `convex/curatedGeometryReconstruct.ts` public `action`/`query` wrappers have **zero** `requireIdentity`. Anyone who can hit the deployment can burn Anthropic/Google quota and rewrite geometry.  
      **Implication**: Acceptable only as spike scaffolding if documented; not shippable.

---

## MEDIUM Confidence Findings (2 Agents Agree)

- [ ] **Finding M1 — Soft-assert test theatre on S1-T2 AC-4/5/6** | Severity: **High** (within S1-T2)  
      Agents: convex-reviewer, orchestrator (grep confirm)  
      **Evidence**: `geometryGatePersist.integration.test.ts` wraps body expects in `if (verifyResult.ok)` at lines ~196, 318, 340, 372, 395, 408, 421. Failed verification reads vacuous-pass. AC-4 suite under-asserts `failedCondition == "degenerate"`.  
      **Implication**: Category-4 test theatre; green suite does not fully discriminate gate/persist failures.

- [ ] **Finding M2 — AC-4 Maestro unproven in sprint gate package** | Severity: **High** (S1-T3)  
      Agents: react-native-ui-reviewer, orchestrator  
      **Evidence**: AC-4 VERIFY requires `RECOVERED_ROUTE_ID=nonexistent-route-slug`. Gate steps 6–7 both take happy path (`fallback … SKIPPED`). Worktree has a separate `maestro-ac4-absent.log` showing fallback COMPLETED, but that is **not** in the sprint gate package / S1-T4 evidence chain. Task STATUS claims “Maestro AC-1/3/4”.  
      **Implication**: Dual-branch YAML ≠ dual execution; AC-4 Done is overclaimed relative to gate artifacts.

- [ ] **Finding M3 — S1-T1 PRIMARY oracles weak against future stubs** | Severity: **Medium**  
      Agents: aisdk-reviewer (+ fakeability audit)  
      **Evidence**: AC-1/AC-2 assert length + non-empty query + confidence enum only — no provider usage/token affinity, no content-affinity on road names, no “ordered” sequence oracle. A future hardcoded anchors array would still GREEN. Current production `extractAnchors` is real (`generateText` + `Output.object`).  
      **Implication**: Implementation OK; regression hardness weak.

- [ ] **Finding M4 — Review geometry still stored and may plot on detail** | Severity: **Medium**  
      Agents: convex-reviewer  
      **Evidence**: FAIL path sets `geometryStatus: 'review'` + `riderReady: false` (not browse-servable) but still upserts polyline `value` into `curated_route_geometry`. `resolveDetailRoutePolyline` prefers side-table value — detail may draw a review line.  
      **Implication**: “Never store as servable” is true for best/nearest; weaker for detail honesty.

- [ ] **Finding M5 — Gate plan vs results type mismatch (steps 6–7)** | Severity: **Medium**  
      Agents: react-native-ui-reviewer, orchestrator  
      **Evidence**: `gate-plan.json` marks steps 6–7 `type: "ui"`; `gate-results.json` records `type: "terminal"`. Steps 6 and 7 run nearly the same Maestro flow twice instead of distinct “tap” vs “observe” human steps.

---

## LOW Confidence Findings (Single Agent)

- [ ] **Finding L1 — Module-level `routingInvocationCount` concurrency hazard** | Severity: **Medium** (within Node action)  
      Agent: convex-reviewer  
      **Evidence**: `let routingInvocationCount = 0` at module scope in `curatedGeometryReconstruct.ts` — concurrent reconstruct actions can corrupt spy counts (engine AC-3 routingCallCount).

- [ ] **Finding L2 — AC-3 scenario string vs fixture (`Foxen Canyon Rd` vs `Road`)** | Severity: **Low**  
      Agent: aisdk-reviewer  
      Scenario MUST_OBSERVE uses abbreviated “Rd”; test/fixture use “Road”.

- [ ] **Finding L3 — SKIP-with-reason can empty-register tests** | Severity: **Medium**  
      Agent: aisdk-reviewer  
      When `ANTHROPIC_API_KEY` absent, named AC tests may not register; `vitest -t "…"` can match 0 tests and exit 0.

- [ ] **Finding L4 — Hardcoded PoC name in Maestro flow** | Severity: **Low**  
      Agent: react-native-ui-reviewer  
      Asserts text `"Twist of Tepusquet Loop"` — couples flow to one catalog name.

- [ ] **Finding L5 — Degenerate 1-pt UI still has string-presence polyline probe; no approximate badge** | Severity: **Low**  
      Agent: react-native-ui-reviewer  
      AC-2 prose implies badge for degenerate; implementation keeps `hasPolyline=true` for 1-pt encoded strings so badge stays off (real-line correctly absent).

- [ ] **Finding L6 — `listCuratedRoutes` bbox/state modes not riderReady-gated** | Severity: **Medium** (product risk; out of S1 AC center)  
      Agent: convex-reviewer  
      Only best (index) + nearest (post-filter) honor riderReady.

- [ ] **Finding L7 — Task writeAllowed omitted public wrapper file** | Severity: **Low**  
      Agent: convex-reviewer  
      `convex/curatedGeometryReconstruct.ts` required for documented CLI; scope drift.

- [ ] **Finding L8 — RED-against-start narrative for S1-T1** | Severity: **Low**  
      Agent: aisdk-reviewer  
      RED proof is narrative (“module didn’t exist”), not a captured failing vitest log.

---

## Gate Pre-Check (skill-emitted, deterministic)

| Check | Result |
|-------|--------|
| Entry points exist as documented | **Partial YES** — public `curatedGeometryReconstruct:reconstructForRoute` action exists (`convex/curatedGeometryReconstruct.ts:11-15`); Maestro yaml exists; `listCuratedRoutes` / `listCuratedRoutesInternal` exist; `maestro` binary on PATH |
| Sprint claims complete | **YES** — `SPRINT.md` `status: Done`; all S1-T1..T4 STATUS Done; no `sprint-goal-state.json` |
| `gate-results.json` present, `verdict:"pass"`, 7/7 executed | **YES** on paper (`run_id: 20260712T072127Z`) |
| Freshness vs source | Evidence timestamps align with Done commit `44e7b2ea` |
| Evidence integrity vs documented steps | **FAIL** — see H1, H3, H4 (re-read theatre; blank map; Maestro≠founder) |

**Auto-findings (gate-pre-check):**

1. **HIGH** — Human testing gate unverified in substance: claimed complete + pass verdict, but steps 1–3 are not reconstruct and step 7 screenshot fails the functional claim.  
2. **HIGH** — Gate step 6 not executable *as worded* (“tap recovered route”): only deep-link automation exists in the flow that was run.  
3. **MEDIUM** — Wiring gap residual: public `listCuratedRoutes` requires Clerk; gate used `listCuratedRoutesInternal` (auth bypass). Documented human path needs identity/client.

---

## Per-Task AC / TC Verdict Matrix

### S1-T1 — Direct AI-SDK anchor extraction

| ID | Verdict | Notes |
|----|---------|-------|
| AC-1 PRIMARY | **PASS** (impl + historical artifacts) | Real `generateText` + `Output.object`; not re-run this session |
| AC-2 | **PASS** | Same real path; Old Hwy 40 fixture |
| AC-3 | **PASS** | Deterministic pure prompt assembly |
| AC-4 | **PASS** | Zod rejects empty anchors / missing confidence |
| TC-1..TC-4 | **PASS** | Map to ACs; residual weak oracles (M3) |

**S1-T1 disposition:** Product code **APPROVE-worthy**. Residual: oracle hardness, SKIP empty-run risk. Not a sprint blocker alone.

### S1-T2 — Geometry reconstruction seam (focus)

| ID | Verdict | Notes |
|----|---------|-------|
| AC-1 PRIMARY | **PASS** (impl + prior evidence) | Real Anthropic+Google path; ratio 1.18 in residual verification; gate did **not** re-run reconstruct |
| AC-2 | **PASS** (impl) / weak riderReady assert | Inclusive band; fixed-geometry → real persist |
| AC-3 | **PASS** | 1-anchor short-circuit; routingCallCount 0; off-region filter |
| AC-4 | **PARTIAL** | Gate real; soft-assert tests; failedCondition under-asserted in suite |
| AC-5 | **PARTIAL** | Ratio-skip real; soft-assert tests |
| AC-6 | **PARTIAL** | failedCondition named in gate; soft-assert + order coupling |
| AC-7 | **PASS** | 7 flips via real mutations; stored riderReady; best walks index |
| TC-1 | **PASS** | Smoke exists; SKIP-with-reason only on outage |
| TC-2 | **PASS** | Boundary ratios through real persist |
| TC-3 | **PASS** | Anchors + routing spy |
| TC-4..TC-6 | **PARTIAL** | Soft ifs |
| TC-7 | **PASS** | riderReady composition |
| TC-8 | **PASS** | Pure gate unit |

**S1-T2 disposition:** **NEEDS_FIXES** (test theatre + unauth public surface). Core seam is **not** a logic stub.

### S1-T3 — Maestro cold-boot plot flow (focus)

| ID | Verdict | Notes |
|----|---------|-------|
| AC-1 PRIMARY | **PARTIAL / FAIL outcome** | Cold-boot + real-line assert GREEN; **screenshot fails visual road-line claim** |
| AC-2 | **PASS** | Vitest: null + 1-pt omit real-line; residual badge prose mismatch for 1-pt |
| AC-3 | **PASS** | stopApp/clearState + chat-input before nav |
| AC-4 | **PARTIAL** | Branch + null query real; **not in sprint gate package** |
| TC-1 | **PARTIAL** | Exit 0 ≠ line painted |
| TC-2 | **PASS** | Branch tests |
| TC-3 | **PASS** | Cold-boot path |
| TC-4 | **PARTIAL** | Separate env required; archive incomplete in gate |

**S1-T3 disposition:** **NEEDS_FIXES**. Discriminator and cold-boot mechanics are real; **plot proof is not**.

### S1-T4 — Founder observe / human gate

| Claim | Verdict | Notes |
|-------|---------|-------|
| Steps 1–5 CLI | **PARTIAL** | 4–5 list queries real; 1–3 re-read only |
| Steps 6–7 observe line | **FAIL** | Maestro substitution + blank map screenshot |
| §5 seam green | **FAIL** | Cannot record green while step7.png shows no line |

**S1-T4 disposition:** **FAIL** as recorded. Manual re-observation required after map paint fix.

---

## Human Testing Gate — Step-by-Step

| Step | Documented action | Runnable entry point? | What gate package did | Verdict |
|------|-------------------|----------------------|------------------------|---------|
| 1 | Reconstruct PoC on real APIs | YES (`curatedGeometryReconstruct:reconstructForRoute`) | `getVerificationForRoute` only | **FAIL as executed** |
| 2 | Gate admits ratio band | YES (verification read) | Same verification read (ratio 1.18 pass) | **PASS residual state only** |
| 3 | Persist ai_reconstructed + riderReady | YES (side-effect of reconstruct) | Same verification read | **FAIL as executed** |
| 4 | listCuratedRoutes best | YES* (public needs identity; internal used) | `listCuratedRoutesInternal` best — PoC present | **PASS** (internal path) |
| 5 | listCuratedRoutes nearest | YES* | Internal nearest @ 34.95,-120.42 — PoC present | **PASS** (internal path) |
| 6 | Cold-boot + tap recovered route | Maestro cold-boot YES; **tap NO** | Maestro deep-link; step6.png is **home**, not detail | **PARTIAL** |
| 7 | Watch correct road line | Visual claim | Maestro real-line testID; **step7.png no line** | **FAIL** |

---

## Agent Contradictions & Debates

| Topic | Position A | Position B | Assessment |
|-------|------------|------------|------------|
| S1-T1 “Done” | Task STATUS + prior aisdk APPROVED | Oracle weakness / not re-run live | **Product PASS; process residual LOW–MED** |
| S1-T2 “Done” | Prior convex APPROVED; real persist evidence on disk | Soft-assert AC-4/5/6 + unauth wrappers | **Impl strong; Done overstated** |
| S1-T3 AC-1 | Maestro exit 0 + real-line visible testID | step7.png blank map | **Automation GREEN is false friend to human gate** |
| Gate `verdict: pass` | gate-verification recomputed pass | Independent evidence review | **Package integrity fails substance** |
| “Servable on FAIL” | riderReady false + status review | Side-table still holds geometry for detail | **Browse OK; detail honesty MEDIUM gap** |

---

## Recommendations by Category

1. **Gaps (blocking)**  
   - Re-run human gate steps 1–3 with an actual `reconstructForRoute` invocation (or prove reconstruct in-session with distinct logs).  
   - Fix map paint / settle so a cold-boot detail view **shows** the copper line; capture a screenshot that fails without it.  
   - Re-run and archive AC-4 Maestro with `nonexistent-route-slug`.  
   - Founder-Operator (or honest visual automated assert) re-signs S1-T4 only after step 7 shows a real line.

2. **Risks**  
   - Add `requireIdentity` (or keep reconstruct internal-only) on public wrappers.  
   - Replace soft `if (verifyResult.ok)` with hard expects in AC-4/5/6.  
   - Consider not resolving review-status side-table polylines onto the detail map.

3. **Assumptions to invalidate or document**  
   - “testID real-line visible ⇒ road line plotted.”  
   - “getVerificationForRoute success ⇒ reconstruct just ran.”  
   - “Maestro 6–7 = founder watched.”

4. **Contradictions to resolve in artifacts**  
   - SPRINT/S1-T* STATUS `Done` vs this review.  
   - `gate-plan` UI steps vs `gate-results` terminal/Maestro.  
   - Task STATUS AC-4 claim vs gate package.

---

## Stub Scan Summary

| Area | Category 1–3 production stubs? | Category 4 test theatre? |
|------|-------------------------------|---------------------------|
| S1-T1 `extractAnchors` | **No** — real Anthropic path | Weak oracles (future-stub risk) |
| S1-T2 gate + reconstruct + persist | **No** — pure gate + Google + DB writes | **Yes** — soft ifs AC-4/5/6 |
| S1-T3 real-line probe | Not a pure stub (coords≥2 real) | **Yes** for “line plots” claim (invisible probe) |
| Human gate package | N/A | **Yes** — steps 1–3 re-read; 6–7 probe pass / blank map |

---

## Agent Reports (Summary)

| Agent | Key findings | Disposition |
|-------|--------------|-------------|
| **aisdk-reviewer** | S1-T1 real v7 path; AC-1..4 PASS; MEDIUM oracle/SKIP risks | APPROVE product; residual process |
| **convex-reviewer** | Seam real; soft-assert HIGH; unauth HIGH; AC-4/5/6 PARTIAL | **NEEDS_FIXES** |
| **react-native-ui-reviewer** | Cold-boot + ≥2 gate real; step7 blank map CRITICAL; AC-4 unproven in gate | **NEEDS_FIXES** |
| **gate-pre-check** | Entry points exist; claimed Done; evidence substance fails | **Do not clear gate** |

---

## Metadata

- **Agents**: aisdk-reviewer, convex-reviewer, react-native-ui-reviewer (parallel, read-only) + orchestrator consolidation  
- **Confidence Framework**: HIGH (3+ agree / orchestrator+image), MEDIUM (2 agree), LOW (1 agent)  
- **Report Generated**: 2026-07-12T07:30:55Z  
- **Duration**: ~8m (dispatch + static review + gate log/image inspection)  
- **Artifacts inspected**: SPRINT + S1-T1..T4 task files; `gate-{plan,results,verification}.json`; `/tmp/laneshadow-gate-sprint-01-geometry-reference-spike/*`; main implementation paths listed above  
- **Files written**: this report only (no code/task STATUS edits)  
- **Next Steps**:  
  1. Remediate Critical/High findings (visual plot + gate evidence + soft tests + auth).  
  2. Re-run `/kb-run-human-tests` with honest steps 1–7.  
  3. Re-run `/review-red-hat` after remediation.  
  4. **Do not mark Sprint 01 / T-REC-016 complete** and **do not unblock Sprint 04** until the human-visible road line is proven.

---

## Bottom Line

| Question | Answer |
|----------|--------|
| Is the geometry reconstruction seam real code? | **Mostly yes** (S1-T1/T2 production paths are not logic stubs) |
| Is the cold-boot Maestro path real? | **Mechanically yes**; **visually no** on captured gate evidence |
| Does the Human Testing Gate pass adversarial review? | **No** |
| Call sprint done? | **No** |
