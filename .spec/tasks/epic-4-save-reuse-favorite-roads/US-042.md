# US-042: Wire long-press segment selection to home map screen

> Status: REDUCED (2026-04-06) — component complete, wiring missing
> Original Commit: d8781dbe (long-press handler built)
> Task ID: US-042
> Type: FEATURE
> Priority: P1
> Estimate: 15 minutes
> Assignee: ui-developer

## WHAT'S DONE

The `RoutePolylineComponent` already has:
- `LongPressGestureHandler` (500ms, react-native-gesture-handler)
- `onSegmentSelect` callback returning `{ geometry, bounds, legIndex, segmentType, segmentId }`
- Haptic feedback on trigger
- Tests passing

## WHAT'S MISSING

The home map screen (`app/(app)/(tabs)/index.tsx`) does **not** pass `onSegmentSelect` to `RoutePolylineComponent`. The callback exists on the component but is never wired.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Route polyline on map | User long-presses segment | `onSegmentSelect` fires with segment data | Manual: long-press, verify callback |
| 2 | Segment selected | Callback received in HomeMapScreen | State is set for SaveFavoriteSheet to consume (US-043) | Code review |

## GUARDRAILS

### WRITE-ALLOWED
- `app/(app)/(tabs)/index.tsx` (MODIFY — pass onSegmentSelect to RoutePolylineComponent)

### WRITE-PROHIBITED
- `components/map/route-polyline-component.tsx` (already done)
- `convex/` (no backend changes)

## CODE PATTERN

```typescript
// In HomeMapScreen
const [selectedSegment, setSelectedSegment] = useState<SegmentSelectData | null>(null)

// Pass to RoutePolylineComponent
<RoutePolylineComponent
  // ... existing props
  onSegmentSelect={setSelectedSegment}
/>
```

## DEPENDENCIES
- US-043 consumes the `selectedSegment` state to show SaveFavoriteSheet
