# US-013: Show route metadata (name, date, locations, distance) on card
> Status: ✅ Completed
> Completed: 2026-03-26T00:00:00Z
> Commit: ff78cf68016be4149339538dfb91f9d890e6a17b
> Reviewer: react-native-ui-reviewer

> Task ID: US-013
> Type: FEATURE
> Priority: P2
> Estimate: 30 minutes
> Assignee: tdd-ui-implementer

## CRITICAL CONSTRAINTS

### MUST
- Display formatted date (e.g., 'Mar 15, 2026') on each card
- Show start and end location labels as the path string
- Use semantic theme tokens for all typography and colors

### NEVER
- Change the SavedRouteCard layout structure (row with thumbnail, details, chevron)
- Add new network calls or Convex queries
- Add moment.js or date-fns as dependencies

### STRICTLY
- Date formatting must use Intl.DateTimeFormat or equivalent (no external date library)
- Date line styled with onSurface.subtle and bodySmall typography

## SPECIFICATION

**Objective:** Enhance SavedRouteCard to display date saved and ensure start/end location labels are shown from the route list data.

**Success looks like:** Each route card displays: route name, formatted date, origin-to-destination path, and distance - all populated from SavedRouteListItemView.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | A saved route with createdAt timestamp, name, and preview data | SavedRouteCard renders | Card shows route name, formatted date (e.g., 'Mar 15, 2026'), and formatted distance (e.g., '12.5km') | `npx vitest run components/ui/saved-route-card.test.tsx` |
| 2 | A saved route with createdAt from today | SavedRouteCard renders | Date shows today's date in the same format (no 'Today' special casing in MVP) | `npx vitest run components/ui/saved-route-card.test.tsx` |
| 3 | A saved route with very long name (50+ characters) | SavedRouteCard renders | Name truncates with ellipsis, card layout does not break | `npx vitest run components/ui/saved-route-card.test.tsx` |
| 4 | A saved route with distance of 0 meters | SavedRouteCard renders | Distance displays as '0m' (no crash or NaN) | `npx vitest run components/ui/saved-route-card.test.tsx` |

## GUARDRAILS

### WRITE-ALLOWED
- components/ui/saved-route-card.tsx (MODIFY)
- components/ui/saved-route-card.test.tsx (NEW)

### WRITE-PROHIBITED
- hooks/use-saved-routes.ts - data layer is complete
- convex/** - backend is complete
- app/(app)/(tabs)/saved-routes.tsx - list wiring is US-010

## DESIGN

### References
- components/ui/saved-route-card.tsx (current implementation)
- types/routes.ts:40-47 (SavedRouteListItemView with createdAt)
- components/sheets/route-details-sheet.tsx:34-52 (formatDistance, formatDuration helpers)

### Interaction Notes
- SavedRouteCard already shows name, path, duration, distance
- Add 'dateSaved' prop (formatted string) with calendar-outline icon in stats row
- Date display: MaterialCommunityIcons 'calendar-outline' at size 14 with semantic.color.onSurface.subtle
- Format at list level: `Intl.DateTimeFormat('en-US', {month:'short', day:'numeric', year:'numeric'}).format(new Date(createdAt))`
- Date line goes between name and path, styled with onSurface.subtle and bodySmall
- Reuse formatDistance/formatDuration pattern from route-details-sheet.tsx

### Code Pattern
Source: components/ui/saved-route-card.tsx:14-27

```typescript
export type SavedRouteCardProps = {
  name: string
  path: string
  dateSaved?: string  // NEW - pre-formatted date string
  duration?: string
  distance?: string
  onPress?: () => void
  thumbnailRotation?: number
}
```

### Anti-pattern (DO NOT)
Do NOT add moment.js or date-fns as dependencies. Do NOT restructure the card layout. Do NOT hardcode colors.

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Named exports
  - Semantic theme tokens
  - No new dependencies

## DEPENDENCIES

No task dependencies.

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | components/ui/saved-route-card.tsx | Current card props and layout |
| Before implementation | types/routes.ts | SavedRouteListItemView.createdAt field |

## NOTES

This is a lightweight card enhancement. The date formatting happens at the list level (saved-routes.tsx), and the card receives a pre-formatted string.
