# US-014: Build empty state component with planning CTA
> Status: ✅ Completed
> Completed: 2026-03-26T00:00:00Z
> Commit: a1161eacab0e01ba066ef035bb51adfed79dda9f
> Reviewer: react-native-ui-reviewer

> Task ID: US-014
> Type: FEATURE
> Priority: P2
> Estimate: 30 minutes
> Assignee: tdd-ui-implementer

## CRITICAL CONSTRAINTS

### MUST
- Create a generic reusable EmptyState component at components/ui/empty-state.tsx
- Display icon, headline text, body text, and a CTA button
- CTA navigates to the Home tab (route planning screen)
- Use semantic theme tokens for all visual styling

### NEVER
- Add route planning logic inside the empty state component
- Use hardcoded colors or spacing values
- Show empty state during loading (show skeleton cards instead)

### STRICTLY
- Component must be generic (not saved-routes-specific) - accepts props for icon, headline, body, ctaLabel, onCtaPress

## SPECIFICATION

**Objective:** Build a reusable EmptyState component and wire it as the ListEmptyComponent for the saved routes FlatList.

**Success looks like:** When user has 0 saved routes, they see an empty state with icon, 'No saved routes yet' heading, descriptive body text, and a 'Plan your first route' button that navigates to Home tab.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | User has 0 saved routes and is on Saved Routes tab | Empty state component renders | Shows a 64px icon, 'No saved routes yet' heading, descriptive text, and 'Plan your first route' button | `npx vitest run components/ui/empty-state.test.tsx` |
| 2 | Empty state is displayed | User taps 'Plan your first route' button | onCtaPress callback is invoked (parent handles navigation to Home tab) | `npx vitest run components/ui/empty-state.test.tsx` |
| 3 | Empty state component is rendered | Screen is in dark mode (default for this app) | All text, icon, and button use semantic theme tokens and are legible | `npx vitest run components/ui/empty-state.test.tsx` |
| 4 | Empty state renders without onCtaPress callback | Component renders | CTA button is hidden (no crash, graceful handling) | `npx vitest run components/ui/empty-state.test.tsx` |

## GUARDRAILS

### WRITE-ALLOWED
- components/ui/empty-state.tsx (NEW)
- components/ui/empty-state.test.tsx (NEW)
- app/(app)/(tabs)/saved-routes.tsx (MODIFY - wire as ListEmptyComponent)

### WRITE-PROHIBITED
- hooks/use-saved-routes.ts - data layer is complete
- components/ui/saved-route-card.tsx - separate component

## DESIGN

### References
- components/ui/button.tsx (Button component for CTA)
- components/sheets/route-details-sheet.tsx:190-209 (Button usage pattern)
- app/(app)/(tabs)/saved-routes.tsx (consumer - ListEmptyComponent)

### Interaction Notes
- Center content vertically and horizontally: flex:1, alignItems:'center', justifyContent:'center'
- Icon: MaterialCommunityIcons 'map-marker-path' at size 64, color semantic.color.onSurface.muted
- Icon is decorative: accessibilityElementsHidden=true
- Headline: Text variant titleMedium, semantic.color.onSurface.default
- Body: Text variant bodyMedium, semantic.color.onSurface.subtle, textAlign:'center'
- Button: variant='default', size='lg', marginTop semantic.space.xl
- Headline + body read by screen reader as a single accessible label
- Parent saved-routes.tsx passes this as FlatList ListEmptyComponent with onCtaPress={() => router.push('/(app)/(tabs)')}

### Code Pattern
Source: components/sheets/route-details-sheet.tsx:57-64

```typescript
export type EmptyStateProps = {
  icon: string           // MaterialCommunityIcons name
  headline: string
  body: string
  ctaLabel?: string
  onCtaPress?: () => void
  testID?: string
}

export const EmptyState = ({ icon, headline, body, ctaLabel, onCtaPress, testID }: EmptyStateProps) => {
  const { semantic } = useSemanticTheme()
  // ...
}
```

### Anti-pattern (DO NOT)
Do NOT make the empty state a full-screen modal or sheet. It should be an inline component within the FlatList. Do NOT hardcode any colors. Do NOT use react-native Text - use react-native-paper Text.

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Named exports
  - Composition over inheritance
  - Semantic theme tokens
  - testID props
  - Accessibility

## DEPENDENCIES

This task depends on:
- US-010 (FlatList must exist to wire ListEmptyComponent)

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | components/ui/button.tsx | Button props and variants |
| Before implementation | app/(app)/(tabs)/saved-routes.tsx | Where EmptyState is consumed |

## NOTES

This creates a generic EmptyState component reusable across the app (future: settings empty states, search no-results). The saved-routes screen provides specific copy and navigation callback.
