# US-012: Add static map thumbnail preview to SavedRouteCard
> Status: ✅ Completed
> Completed: 2026-03-26T00:00:00Z
> Commit: 68665d850d71e70c9d739274341c1a87da094ba6
> Reviewer: react-native-ui-reviewer

> Task ID: US-012
> Type: FEATURE
> Priority: P2
> Estimate: 40 minutes
> Assignee: tdd-ui-implementer

## CRITICAL CONSTRAINTS

### MUST
- Use the existing RouteThumbnail component as the base
- Fix the invalid CSS linear-gradient backgroundColor (replace with expo-linear-gradient LinearGradient)
- Thumbnail must render at 96x96px with dark gradient background
- Use semantic theme tokens for all colors

### NEVER
- Fetch map tiles or make network requests for the thumbnail
- Modify the FlatList rendering logic in saved-routes.tsx beyond passing thumbnail props

### STRICTLY
- RouteThumbnail enhancements stay within components/ui/route-thumbnail.tsx
- All new props must be optional (backward-compatible)

## SPECIFICATION

**Objective:** Fix RouteThumbnail's invalid CSS gradient and enhance it to accept route bounds/geometry data for a more representative static preview on each saved route card.

**Success looks like:** Each saved route card shows a 96x96 thumbnail with a proper dark gradient background and a copper-colored route line that varies based on the route's overview geometry.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | A saved route with valid overviewGeometry in its snapshot | RouteThumbnail renders with the route data | Thumbnail displays a stylized route line at 96x96 with dark gradient background (expo-linear-gradient) and copper route color | `npx vitest run components/ui/route-thumbnail.test.tsx` |
| 2 | RouteThumbnail is rendered with no geometry props (backwards compatible) | Component renders | Falls back to default decorative route line (current behavior preserved) with fixed gradient background | `npx vitest run components/ui/route-thumbnail.test.tsx` |
| 3 | Route bounds span a very small area (< 0.01 degrees) | RouteThumbnail renders | Route line is still visible and centered within the thumbnail (no zero-size rendering) | `npx vitest run components/ui/route-thumbnail.test.tsx` |
| 4 | RouteThumbnail receives undefined or malformed geometry | Component renders | Renders gracefully with fallback decorative line, no crash | `npx vitest run components/ui/route-thumbnail.test.tsx` |

## GUARDRAILS

### WRITE-ALLOWED
- components/ui/route-thumbnail.tsx (MODIFY)
- components/ui/route-thumbnail.test.tsx (NEW)

### WRITE-PROHIBITED
- components/ui/saved-route-card.tsx - card metadata changes are US-013
- app/(app)/(tabs)/saved-routes.tsx - list screen wiring is US-010

## DESIGN

### References
- components/ui/route-thumbnail.tsx (current implementation with CSS gradient bug)
- components/sheets/route-timeline.tsx (expo-linear-gradient usage pattern to follow)
- models/saved-routes.ts:284-295 (routeSnapshotValidator - bounds, overviewGeometry)
- types/routes.ts:40-47 (SavedRouteListItemView with preview.bounds)

### Interaction Notes
- RouteThumbnail currently assigns 'linear-gradient(135deg, #1a1d21 0%, #0d1117 100%)' as backgroundColor - this is INVALID in React Native (renders transparent/black)
- Replace with expo-linear-gradient LinearGradient wrapper: colors=['#1a1d21', '#0d1117'], start={x:0,y:0}, end={x:1,y:1}
- expo-linear-gradient is already used in RouteTimeline component (no new dependency)
- Enhancement should accept optional bounds prop to derive rotation/positioning from actual route data
- Keep the existing props interface backward-compatible (all new props optional)

### Code Pattern
Source: components/ui/route-thumbnail.tsx:12-27

```typescript
export type RouteThumbnailProps = {
  width?: number
  height?: number
  rotation?: number
  bounds?: Bounds  // NEW optional prop
}
```

### Anti-pattern (DO NOT)
Do NOT use MapView or any map SDK for thumbnails - this is a pure CSS/SVG visualization. Do NOT make the thumbnail interactive. Do NOT use CSS gradient strings in React Native StyleSheet.

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Named exports
  - Composition over inheritance
  - Semantic theme tokens

## DEPENDENCIES

No task dependencies.

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | components/ui/route-thumbnail.tsx | Current implementation and CSS gradient bug |
| Before implementation | components/sheets/route-timeline.tsx | expo-linear-gradient usage pattern |

## NOTES

The RouteThumbnail has a pre-existing bug: CSS `linear-gradient()` strings are invalid as React Native `backgroundColor` values. This task fixes that bug while enhancing the component.
