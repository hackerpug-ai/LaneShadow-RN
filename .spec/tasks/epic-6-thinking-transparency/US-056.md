# US-056: Add Thinking Card Lifecycle Mutations

> Status: COMPLETED
> Task ID: US-056
> Type: FEATURE
> Priority: P0
> Estimate: 60 minutes
> Assignee: convex-implementer
> Reviewer: convex-reviewer

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | No thinking card exists | `createThinkingCard({ sessionId })` called | Creates row with `kind: 'thinking_card'`, `status: 'streaming'`, `content: ''`, `thinkingSteps: []` | ✅ PASS - convex/db/sessionMessages.ts:426-434 |
| 2 | Thinking card exists | `appendThinkingText({ messageId, delta: 'hello' })` called | `content` field appended with delta | ✅ PASS - convex/db/sessionMessages.ts:450 |
| 3 | Thinking card exists with 0 steps | `appendThinkingStep({ messageId, step: {...} })` called | `thinkingSteps` array has 1 element | ✅ PASS - convex/db/sessionMessages.ts:465-467 |
| 4 | Thinking card with 3 steps | `appendThinkingStep` called again | `thinkingSteps` array has 4 elements, order preserved | ✅ PASS - Test coverage at thinkingCard.test.ts:207-243 |
| 5 | Thinking card streaming | `finalizeThinkingCard({ messageId })` called | `status` set to `'complete'` | ✅ PASS - convex/db/sessionMessages.ts:482 |
| 6 | `createThinkingCard` | Returns | `{ messageId: Id<'session_messages'> }` | ✅ PASS - convex/db/sessionMessages.ts:436 |

## REVIEW OUTCOME

**Verdict**: APPROVED
**Reviewer**: convex-reviewer
**Date**: 2026-04-08
**Commit**: 5bcff8314e52027caaf0887ed2c5d181c55e0dc6

### Review Summary
All 4 internal mutations implemented correctly following existing patterns in sessionMessages.ts:
- createThinkingCard: Creates row with proper initial state
- appendThinkingText: Appends delta to content
- appendThinkingStep: Appends steps to array with order preservation
- finalizeThinkingCard: Sets status to complete

### Verification Results
- Tests: 12 pass, 0 fail
- Typecheck: 0 errors
- Lint: 0 new warnings (1119 pre-existing)
- No stubs detected
- All validation gates passed

### Implementation Notes
- Uses internalMutation (not public-facing)
- Uses v validators from convex/values
- Follows read-then-patch pattern for array mutations
- Proper error handling with ConvexError
- Updates parent session updatedAt timestamps
