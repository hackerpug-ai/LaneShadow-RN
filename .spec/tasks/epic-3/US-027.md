# US-027: Wire search + date filter into saved-routes screen
> Status: ✅ Completed
> Completed: 2026-03-27T00:00:00Z
> Commit: 64388da0bf9e1e62be466c63553cf884f1933c2a
> Reviewer: react-native-ui-reviewer

> Task ID: US-027
> Type: FEATURE
> Priority: P1
> Estimate: 60 minutes
> Assignee: ui-developer

## CRITICAL CONSTRAINTS

### MUST
- Add RouteSearchBar and DateRangePicker to saved-routes screen header area
- Pass searchQuery and date range params to `useSavedRoutesList` hook
- Update `useSavedRoutesList` hook to accept and forward `searchQuery`, `afterDate`, `beforeDate` args
- Add a "Clear all filters" button that resets both search and date filters
- Show result count when filters are active (e.g., "3 routes found")

### NEVER
- Duplicate filtering logic in the frontend (backend handles all filtering)
- Hardcode any colors, spacing, or font sizes
- Break existing scroll position preservation behavior
- Add new Convex queries (use the extended existing query)

### STRICTLY
- "Clear all filters" only visible when at least one filter is active
- Screen file must stay under 150 lines (extract sub-components if needed)
- Filter state managed with `useState` hooks in the screen component
- All new visual elements use semantic theme tokens

## SPECIFICATION

**Objective:** Wire the search bar and date range picker into the saved routes screen, connecting them to the backend query through the existing hook.

**Success looks like:** User can type in search bar, select date presets, and see the route list update in real-time. A "Clear all filters" button resets everything. Result count shown when filtered.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | User is on Saved Routes tab with routes | User types "park" in search bar | After 300ms debounce, list updates to show only routes with "park" in name | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |
| 2 | User is on Saved Routes tab | User taps "Last week" date chip | List updates to show only routes created in the last 7 days | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |
| 3 | Search and date filter are both active | User taps "Clear all filters" | Search bar clears, date resets to "All time", full route list returns | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |
| 4 | Filters are active showing 3 results | User views the list | Shows "3 routes found" count label below filters | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |
| 5 | No filters active | User views the list | "Clear all filters" button is NOT visible, result count NOT shown | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |
| 6 | Filters active, 0 results | User views the list | Shows empty state with "No routes match your filters" message | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts` |

## GUARDRAILS

### WRITE-ALLOWED
- app/(app)/(tabs)/saved-routes.tsx (MODIFY - add filter UI and state)
- hooks/use-saved-routes.ts (MODIFY - add filter args to useSavedRoutesList)
- app/(app)/(tabs)/saved-routes.components.tsx (MODIFY - add filter header sub-components if needed)
- app/(app)/(tabs)/__tests__/saved-routes.filter.test.ts (NEW)

### WRITE-PROHIBITED
- convex/** - backend already handles filtering (US-019, US-020)
- components/ui/route-search-bar.tsx - already built (US-023)
- components/ui/date-range-picker.tsx - already built (US-024)
- types/routes.ts - no type changes

## DESIGN

### References
- app/(app)/(tabs)/saved-routes.tsx (current screen implementation)
- hooks/use-saved-routes.ts:28-41 (useSavedRoutesList hook to extend)
- components/ui/route-search-bar.tsx (search bar component from US-023)
- components/ui/date-range-picker.tsx (date picker component from US-024)

### Interaction Notes
- Search bar goes directly below the "Saved Routes" title
- Date range picker goes below the search bar
- "Clear all filters" is a small text button below the date picker, only shown when filters active
- Result count ("N routes found") displayed as subtle text between filters and list
- When filtered to 0 results, show a different empty state than the "no saved routes" one
- Layout order: Title -> Search bar -> Date chips -> Clear filters -> Result count -> FlatList

### Code Pattern
Source: hooks/use-saved-routes.ts (extend hook args)

```typescript
// Updated hook signature:
type SavedRoutesListArgs = {
  limit?: number
  searchQuery?: string
  afterDate?: number
  beforeDate?: number
}

export const useSavedRoutesList = (
  args?: SavedRoutesListArgs
): { data: SavedRoutesListView | undefined; isLoading: boolean } => {
  const data = useQuery(api.db.savedRoutes.getSavedRoutesList, {
    limit: args?.limit,
    searchQuery: args?.searchQuery,
    afterDate: args?.afterDate,
    beforeDate: args?.beforeDate,
  })
  // ...
}
```

### Anti-pattern (DO NOT)
Do NOT filter in the frontend (backend query handles it). Do NOT add inline styles with hardcoded values. Do NOT exceed 150 lines in saved-routes.tsx (extract to .components.tsx).

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Named exports
  - Semantic theme tokens
  - Screen files under 150 lines
  - Extract sub-components to .components.tsx

## DEPENDENCIES

- US-019 (backend search query)
- US-020 (backend date filter)
- US-023 (search bar component)
- US-024 (date range picker component)

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | app/(app)/(tabs)/saved-routes.tsx | Current screen structure |
| Before implementation | hooks/use-saved-routes.ts | Hook to extend |

## NOTES

This is the main integration task for the search/filter feature. It connects all the pieces: backend queries (US-019, US-020) + UI components (US-023, US-024) into the saved routes screen. Keep the screen file lean by extracting the filter header area into saved-routes.components.tsx.
