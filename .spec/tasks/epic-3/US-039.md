# US-039: Memoize renderRightActions in SwipeableRouteCard
> Status: ✅ Completed
> Completed: 2026-03-27T15:31:31Z
> Commit: 0cbdfce88eaf84926f1a9ebd05a34d54f09796b0
> Reviewer: react-native-ui-reviewer

> Task ID: US-039
> Type: FEATURE
> Priority: P2
> Estimate: 15 minutes
> Assignee: ui-developer
> Source: Red-hat review M11 (react-native-ui-reviewer)

## CRITICAL CONSTRAINTS

### MUST
- Wrap `renderRightActions` in `useCallback` in `SwipeableRouteCard` component
- Ensure the memoized callback has correct dependency array
- Verify swipe-to-delete still works after memoization

### NEVER
- Change the swipe behavior or delete flow
- Remove the `renderRightActions` entirely
- Add unnecessary dependencies to the useCallback array

### STRICTLY
- `renderRightActions` must be referentially stable across re-renders
- Swipeable must not remeasure action width on every parent re-render
- Search bar typing must not cause swipeable re-measurements

## SPECIFICATION

**Objective:** Prevent unnecessary re-measurements of swipe action widths during FlatList re-renders caused by search/filter changes.

**Success looks like:** Typing in the search bar does not cause visible jank or layout recalculation in swipeable route cards.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Multiple route cards rendered with swipeable | User types in search bar | Swipeable components do not remeasure (no layout jank) | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.test.ts` |
| 2 | A route card is partially swiped open | Parent re-renders from search | Swipe position is maintained (no snap-back) | Manual test |
| 3 | `renderRightActions` in SwipeableRouteCard | Inspecting code | Wrapped in `useCallback` with correct deps | Code review |
| 4 | Swipe-to-delete flow | User swipes left and taps delete | Delete dialog appears and flow works | `npx vitest run app/(app)/(tabs)/__tests__/saved-routes.test.ts` |

## GUARDRAILS

### WRITE-ALLOWED
- app/(app)/(tabs)/saved-routes.components.tsx (MODIFY - wrap renderRightActions in useCallback)
- app/(app)/(tabs)/__tests__/saved-routes.test.ts (MODIFY - if needed)

### WRITE-PROHIBITED
- convex/** - no backend changes
- components/** - no component changes
- hooks/** - no hook changes

## DESIGN

### References
- app/(app)/(tabs)/saved-routes.components.tsx:220 (inline renderRightActions to memoize)
- app/(app)/(tabs)/saved-routes.components.tsx:200-260 (SwipeableRouteCard component)

### Code Pattern
Source: React useCallback pattern

```typescript
const renderRightActions = useCallback(
  (progress: Animated.AnimatedInterpolation<number>) => (
    <Pressable
      onPress={onDelete}
      style={[styles.deleteAction, { backgroundColor: semantic.color.danger.default }]}
    >
      <MaterialCommunityIcons name="trash-can-outline" ... />
    </Pressable>
  ),
  [onDelete, semantic]
)
```

### Anti-pattern (DO NOT)
Do NOT over-memoize by wrapping every function -- only `renderRightActions` needs this because it's passed to Swipeable's render prop. Do NOT add state to the dependency array that changes on every render.

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Memoize render callbacks passed to child components
  - Correct dependency arrays

## DEPENDENCIES

No task dependencies.

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | app/(app)/(tabs)/saved-routes.components.tsx:200-260 | SwipeableRouteCard and renderRightActions |

## NOTES

Red-hat finding M11: react-native-ui-reviewer identified that the inline non-memoized `renderRightActions` forces Swipeable to remeasure on every search keystroke. One-line fix with `useCallback`.
