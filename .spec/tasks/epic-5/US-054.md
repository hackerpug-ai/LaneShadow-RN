# US-054: Update Test Suite for Deterministic Pipeline

> Status: ✅ Completed
> Completed: 2026-04-02T00:00:00Z
> Commit: 8c10344262b7c91f9ffb0e58b045ca19da0c7bb9
> Reviewer: superpowers:code-reviewer

> Task ID: US-054
> Type: TEST
> Priority: P1
> Estimate: 90 minutes
> Assignee: backend-engineer
> Reviewer: convex-reviewer

## CRITICAL CONSTRAINTS

### MUST
- Read ALL existing test files in `convex/actions/agent/__tests__/` before touching them
- Delete tests that mock `createAgentSession`, `piObserver`, or `routePlanningExtension`
- Add unit tests for `findScenicWaypoints` (Overpass parsing + clustering + fallback)
- Add unit tests for `enrichRoute` (prompt building + fallback on error)
- Add unit tests for `planRideOrchestrator` (happy path + partial failure + timeout)

### NEVER
- Delete tests for `compileSketch`, `normalizeRoute`, `computeRouteIndex`, `probeConditions`, `mapConditions` — these tools are unchanged
- Remove the `routingProvider.test.ts` file — add to it instead

### STRICTLY
- All new tests follow existing jest patterns in the codebase
- Mock Overpass API via `jest.spyOn(global, 'fetch')` or a fetchMock utility

## SPECIFICATION

**Objective:** After US-051 removes the pi agent infrastructure, update the test suite to reflect the deterministic pipeline. Remove stale mocks, add tests for new tools, ensure full coverage of the happy path and key error scenarios.

**Success looks like:** `pnpm test` passes with zero skipped tests. No tests reference `createAgentSession`, `piSession`, `piObserver`, or `routePlanningExtension`.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | All test files | `pnpm test` run | All tests pass, 0 failures | Terminal output |
| 2 | `createAgentSession` reference | After task | grep finds 0 occurrences in test files | `grep -r 'createAgentSession' --include='*.test.ts'` |
| 3 | `findScenicWaypoints.test.ts` | Added | Tests: happy path, empty result fallback, timeout fallback, clustering | Test file exists with ≥6 test cases |
| 4 | `enrichRoute.test.ts` | Added | Tests: happy path, malformed JSON fallback, timeout fallback, env model override | Test file exists with ≥5 test cases |
| 5 | `planRide.test.ts` | Updated | Tests: happy path, all variants fail, timeout, cancellation | Updated test file |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/actions/agent/__tests__/planRide.test.ts` (MODIFY)
- `convex/actions/agent/tools/__tests__/findScenicWaypoints.test.ts` (NEW)
- `convex/actions/agent/tools/__tests__/enrichRoute.test.ts` (NEW)
- `convex/actions/agent/providers/__tests__/routingProvider.test.ts` (MODIFY — add TWO_WHEELER tests)

### WRITE-PROHIBITED
- `convex/actions/agent/tools/__tests__/compileSketch.test.ts` (if exists — do not modify)
- `convex/actions/agent/tools/__tests__/normalizeRoute.test.ts` (if exists — do not modify)
- Any source files (tests only)

## DESIGN

### `findScenicWaypoints.test.ts` Coverage

```
describe('findScenicWaypoints', () => {
  it('returns clustered variants from valid Overpass response')
  it('scores passes higher than viewpoints')
  it('returns fallback on Overpass network error')
  it('returns fallback on Overpass timeout')
  it('returns fallback when <2 nodes in response')
  it('skips nodes with no name tag')
  it('computes bbox with correct padding')
})
```

### `enrichRoute.test.ts` Coverage

```
describe('enrichRoute', () => {
  it('returns labels for 3 routes from valid OpenAI response')
  it('uses ENRICH_MODEL env var when set')
  it('returns fallback labels on JSON parse error')
  it('returns fallback labels on OpenAI timeout')
  it('returns fallback labels when OPENAI_API_KEY missing')
})
```

### Updated `planRide.test.ts` Coverage

Remove:
- Any `jest.mock('../lib/piSession')` or similar
- Tests that mock `agentSession.prompt`

Add:
- Mock `planRideOrchestrator` for `executePlanHandler` tests
- Test: `planRideOrchestrator` throws `ROUTING_ALL_VARIANTS_FAILED` → plan status set to `failed`
- Test: timeout after 240s → plan status set to `failed` with `AGENT_TIMEOUT`
- Test: cancellation during planning → early return, no status update

## CODING STANDARDS

- All fetch mocks via `jest.spyOn(global, 'fetch').mockResolvedValue(...)` pattern
- Reset mocks with `afterEach(() => jest.restoreAllMocks())`
- No `any` in test assertion types

## DEPENDENCIES

- US-051: planRide refactor must be complete
- US-052: enrichRoute tool must exist

## REQUIRED READING

1. `convex/actions/agent/__tests__/planRide.test.ts` — existing tests to understand current mock patterns
2. All existing `*.test.ts` files in `convex/actions/agent/tools/__tests__/` — follow their patterns

## NOTES

- Jest is already configured in this project (see `jest.config.ts`)
- The test environment runs in Node (not browser) — `fetch` is available globally in Node 18+
- If a `__tests__` folder doesn't exist under `tools/`, create it following the pattern in `providers/__tests__/`
