# US-056: Create route_enrichments table and schema

> Task ID: US-056
> Type: INFRA
> Priority: P0
> Estimate: 60 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Follow same validator-first pattern as waypoints
- Create indexes for cache lookups
- Support scheduled job tracking
- Use proper status union types

### NEVER
- Mix enrichment status with route plan status
- Store enrichment results directly in route_plans table
- Create without understanding invalidation needs

### STRICTLY
- Content fingerprint must be string (hash)
- Support both 'fast' and 'extended' phases
- Track scheduledJobId for cancellation

## SPECIFICATION

**Objective:** Create database table for tracking background enrichment jobs with cache support and invalidation.

**Success looks like:** route_enrichments table exists with proper indexes for cache lookups and job tracking.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Schema file is modified | route_enrichments validator added | All fields validated properly | `grep "routeEnrichmentValidator" models/` |
| 2 | Table is defined | Indexes created | Can query by fingerprint and phase | `grep "by_contentFingerprint" convex/schema.ts` |
| 3 | Status field | Union type defined | Supports all required statuses | `grep "v.union.*literal.*pending.*running" models/` |
| 4 | TypeScript check | Schema compiles | No type errors in schema | `bun run typecheck` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | route_enrichments table exists in schema when schema file read | AC-2 | `grep -c "route_enrichments:" convex/schema.ts` | [ ] TRUE [ ] FALSE |
| 2 | contentFingerprint field exists when validator inspected | AC-1 | `grep "contentFingerprint" models/*enrichment*.ts` | [ ] TRUE [ ] FALSE |
| 3 | Index by_contentFingerprint_and_phase exists when table checked | AC-2 | `grep "by_contentFingerprint_and_phase" convex/schema.ts` | [ ] TRUE [ ] FALSE |
| 4 | scheduledJobId field is optional when validator structure reviewed | AC-3 | `grep "scheduledJobId.*v.optional" models/*enrichment*.ts` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/schema.ts` (MODIFY)
- `models/route-enrichments.ts` (NEW)

### WRITE-PROHIBITED
- Database operation files - those come later
- Enrichment logic files - schema only

## DESIGN

### References
- Plan: `/Users/justinrich/.claude/plans/imperative-popping-sparrow.md` - Feature 2 Data Model
- Existing: `convex/schema.ts` - route_plans pattern

### Pattern
```typescript
route_enrichments: defineTable(routeEnrichmentValidator)
  .index('by_routePlanId', ['routePlanId'])
  .index('by_contentFingerprint_and_phase', ['contentFingerprint', 'phase'])
```

## DEPENDENCIES

None - can run in parallel with US-050

## REQUIRED READING

- Plan file - Feature 2, Data Model section
- `convex/schema.ts` - existing table patterns

## NOTES

This table enables:
- Background job tracking
- Cache invalidation via fingerprint comparison
- Scheduled job cancellation
- Multi-phase enrichment (fast → extended)
