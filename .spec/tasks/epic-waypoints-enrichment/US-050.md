# US-050: Create waypoints data model and schema

> Task ID: US-050
> Type: INFRA
> Priority: P0
> Estimate: 90 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Read `/Users/justinrich/Projects/LaneShadow/convex/schema.ts` for existing schema patterns
- Follow validator-first pattern from `~/Projects/brain/docs/convex-rules.md`
- Use Convex `v` validators for all fields
- Create proper indexes for query patterns

### NEVER
- Modify existing tables without understanding their usage
- Add fields without proper validators
- Create indexes without understanding query patterns

### STRICTLY
- Keep waypoint table schema under 50 lines
- Follow naming conventions: `waypoints` table, `by_routePlanId` index
- Use `v.id()` for foreign key references

## SPECIFICATION

**Objective:** Create the waypoints database table with full schema support for storing, querying, and managing user waypoints.

**Success looks like:** Waypoints table exists in schema with validators, indexes, and can be queried via Convex functions.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Schema file is open | Waypoints validator is added | All fields have proper Convex validators | `grep -A 20 "waypointValidator" convex/schema.ts` |
| 2 | Waypoints table defined | Indexes are added | Queries can filter by routePlanId and status | `grep "by_routePlanId" convex/schema.ts` |
| 3 | New migration applied | Database is deployed | Waypoints table is queryable | `npx convex dashboard` |
| 4 | Validator patterns | Following existing patterns | No TypeScript errors in schema | `bun run typecheck` |

## TEST CRITERIA

Review agents verify ALL test criteria are TRUE before marking task complete.

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Waypoints table exists in schema when schema file is read | AC-1 | `grep -c "waypoints:" convex/schema.ts` | [ ] TRUE [ ] FALSE |
| 2 | All waypoint fields use Convex v validators when validator is inspected | AC-1 | `grep -A 30 "waypointValidator" convex/schema.ts | grep -c "v\."` | [ ] TRUE [ ] FALSE |
| 3 | Index by_routePlanId exists when table definition is checked | AC-2 | `grep "by_routePlanId" convex/schema.ts` | [ ] TRUE [ ] FALSE |
| 4 | TypeScript compilation succeeds when schema is validated | AC-4 | `bunx tsc --noEmit` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/schema.ts` (MODIFY)
- `models/waypoints.ts` (NEW)

### WRITE-PROHIBITED
- `convex/db/*` - database operations created in next task
- Other model files - focus only on waypoints

## DESIGN

### References
- Plan file: `/Users/justinrich/.claude/plans/imperative-popping-sparrow.md` - Feature 1 Data Model section
- Existing schema: `convex/schema.ts` - patterns from `route_plans` table

### Interaction Notes
- Waypoints will be created in "pending" status
- Status workflow: pending → evaluating → ready → approved/rejected → applied
- On-route waypoints have `order` field for sequencing
- Off-route waypoints have `detourInfo` object

### Code Pattern
```typescript
// From convex/schema.ts - route_plans table pattern
route_plans: defineTable(routePlanValidator)
  .index('by_clerkUserId_and_status', ['clerkUserId', 'status'])
  .index('by_planningSessionId_and_status', ['planningSessionId', 'status'])
```

Apply similar pattern for waypoints:
```typescript
waypoints: defineTable(waypointValidator)
  .index('by_routePlanId', ['routePlanId'])
  .index('by_routePlanId_and_status', ['routePlanId', 'status'])
```

### Anti-pattern (DO NOT)
Don't create the table without proper indexes - queries will be slow.
Don't use `v.optional()` incorrectly - make required fields explicit.

## CODING STANDARDS

- **`~/Projects/brain/docs/convex-rules.md`**:
  - Always use validators from `models/` directory
  - Define validators in `models/` before using in schema
  - Use `v.id()` for foreign key references

## DEPENDENCIES

No task dependencies.

## REQUIRED READING

- `/Users/justinrich/.claude/plans/imperative-popping-sparrow.md` - Feature 1 Data Model section
- `convex/schema.ts` - existing schema patterns
- `~/Projects/brain/docs/convex-rules.md` - Convex best practices

## NOTES

This task creates the foundation for all waypoint functionality. The schema must support:
- On-route vs off-route classification
- Workflow status tracking
- Deviation information storage
- Proper indexing for efficient queries
