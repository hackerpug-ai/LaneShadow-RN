# US-002: Wire temperature-badge.tsx to RouteSnapshot.overlays.temperature

> Task ID: US-002
> Type: FEATURE
> Priority: P1
> Appetite Fit: small (fits)
> Assignee: react-native-ui-implementer
> PRD Version: 1.0.0
> Stability: stable
> Status: ✅ Completed
> Completed: 2026-03-04T00:00:00Z
> Commit: 480582a0ea12d4dd380c24b361937435653c837b
> Reviewer: react-native-ui-reviewer

## CRITICAL CONSTRAINTS

### MUST
- Use existing TemperatureBadge component from components/ui/temperature-badge.tsx
- Derive TemperatureSummary from RouteSnapshot.overlays.temperature data
- Display temperature badge on RouteOptionCard
- Show high/low temps when available, fall back to summary level

### NEVER
- Create a new badge component - reuse existing TemperatureBadge
- Hardcode temperature ranges - derive from actual overlay data

### STRICTLY
- Follow extensible enum pattern from wind-badge.tsx
- Temperature display in Fahrenheit for US locale

## SPECIFICATION

**Objective:** Display temperature badge on each route option card by wiring existing TemperatureBadge component to RouteSnapshot.overlays.temperature data.

**Success looks like:** Route option cards display a temperature badge showing cold/mild/warm/hot/unavailable based on temperature conditions along the route.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | A planned route with temperature overlay data showing comfortable temps (65-75F) | The route option card is rendered | TemperatureBadge displays 'Mild' with green styling | `pnpm vitest run components/planning/route-option-card.test.tsx --grep 'temperature badge mild'` |
| 2 | A planned route with temperature values available | The route option card is rendered | TemperatureBadge shows actual high temp (e.g., '85F') not just level | `pnpm vitest run components/planning/route-option-card.test.tsx --grep 'temperature value'` |
| 3 | A planned route with no temperature overlay data | The route option card is rendered | TemperatureBadge displays 'Unknown' with muted styling | `pnpm vitest run components/planning/route-option-card.test.tsx --grep 'unavailable temp'` |
| 4 | Temperature data contains extreme cold (<40F) or hot (>90F) values | The component derives temperature summary | Badge displays correct extreme level (cold/hot) with appropriate color | `pnpm vitest run components/planning/route-option-card.test.tsx --grep 'extreme temp'` |

## GUARDRAILS

### WRITE-ALLOWED
- components/planning/route-option-card.tsx (MODIFY)
- components/planning/route-option-card.test.tsx (MODIFY)
- types/routes.ts (MODIFY)
- models/saved-routes.ts (MODIFY)
- stories/components/RouteOptionCard.stories.tsx (MODIFY)

### WRITE-PROHIBITED
- components/ui/temperature-badge.tsx - reuse as-is
- convex/** - no backend changes in this task

## DESIGN

### References
- /Users/justinrich/Projects/LaneShadow/components/ui/temperature-badge.tsx
- /Users/justinrich/Projects/LaneShadow/components/planning/wind-badge.tsx

### Interaction Notes
- Temperature badge appears in stats row with Wind and Rain badges
- When temperature value is available, show number (e.g., '85F')

### Code Pattern
Source: components/ui/temperature-badge.tsx:24-55

```typescript
const deriveTempSummary = (tempC: number): TemperatureSummary => {
  if (tempC < 5) return 'cold';
  if (tempC < 20) return 'mild';
  if (tempC < 30) return 'warm';
  return 'hot';
};
```

### Anti-pattern (DO NOT)
Do NOT assume temperature units - always check unit field and convert

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Named exports
  - Story co-location

## DEPENDENCIES

This task depends on:
- US-001

## APPETITE FIT

- **Fits Appetite**: Yes
- **Complexity**: small

## METADATA

- **Source PRD Version**: 1.0.0
- **Source PRD Section**: UC-P1GAP-01
- **Stability**: stable
- **Change Trigger**: overlay_data_format_change

## REQUIRED READING

| When | Document | Focus |
|------|----------|-------|
| Before implementation | components/ui/temperature-badge.tsx | Existing badge API |
| Before implementation | US-001 implementation | Pattern established |

## NOTES

Similar to US-001 but for temperature data. Follows the same wiring pattern.
