# TASK: US-046 - Implement "Include Favorite Roads" Toggle

**Task Type**: FEATURE
**Priority**: P0
**Estimate**: 60 minutes

## CRITICAL CONSTRAINTS

### MUST
- Toggle MUST render in PlanRideSheet above route options
- Toggle MUST default to OFF state
- Toggle state MUST persist during planning session (not across sessions)
- Toggle MUST pass boolean to planRide action via includeFavorites parameter

### NEVER
- NEVER persist toggle state across app sessions (always reset to OFF)
- NEVER enable favorites by default (opt-in only)
- NEVER bypass existing PlanRideSheet validation

### STRICTLY
- STRICTLY follow existing toggle component patterns in the app
- STRICTLY use the app's standard toggle component (no custom implementation)
- STRICTLY ensure toggle state is included in planRide mutations

## SPECIFICATION

**Objective**: Add a toggle to PlanRideSheet that allows users to opt-in to including their favorite roads in route planning.

**Success looks like**: Toggle is visible in planning UI, defaults to OFF, and successfully passes the includeFavorites boolean to the backend planRide action.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | User is planning a ride | User opens PlanRideSheet | "Include favorite roads" toggle is visible above route options | Visual inspection of PlanRideSheet |
| 2 | User opens PlanRideSheet for first time | Toggle renders | Toggle is in OFF position | Visual inspection |
| 3 | User enables toggle | User taps toggle | Toggle switches to ON with animation | Visual inspection + haptic feedback |
| 4 | Toggle is ON | User plans a route | planRide action receives includeFavorites: true | Check network tab / mutation args |
| 5 | Toggle is OFF | User plans a route | planRide action receives includeFavorites: false | Check network tab / mutation args |
| 6 | User plans with toggle ON | User returns to PlanRideSheet | Toggle state persists (ON) | Visual inspection |
| 7 | User dismisses and reopens sheet | Sheet reopens | Toggle resets to OFF (session boundary) | Visual inspection |

## GUARDRAILS

### WRITE-ALLOWED
- `src/features/plan-ride/PlanRideSheet.tsx`
- `src/features/plan-ride/types.ts`
- `src/features/plan-ride/__tests__/PlanRideSheet.test.tsx`

### WRITE-PROHIBITED
- `convex/functions/favoriteRoads.ts` - backend changes in US-047
- `src/components/Toggle.tsx` - use existing component
- Any other UI components outside PlanRideSheet context

## DEPENDENCIES

- None (can start immediately)

## NOTES

**Component Location**:
- PlanRideSheet is likely at `src/features/plan-ride/PlanRideSheet.tsx` or similar
- Look for existing toggle patterns in the codebase

**Implementation Tips**:
1. Find the app's standard Toggle component
2. Add toggle state to PlanRideSheet component
3. Pass includeFavorites to the planRide mutation call
4. Ensure toggle is placed prominently above route options
5. Add descriptive label: "Include favorite roads in route planning"

**Testing**:
- Unit test for toggle state management
- Integration test for planRide mutation with includeFavorites parameter
- Visual regression test for toggle placement
