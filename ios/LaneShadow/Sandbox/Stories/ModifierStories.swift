import LaneShadowTheme
import NativeSandbox
import SwiftUI

/// Modifier stories — Style modifier wrappers.
@MainActor
enum ModifierStories {
    static let all: [Story] = {
        var stories: [Story] = []

        // Modifier stories (optional, rarely used)
        // For now, this tier aggregator is empty (sandbox boots cleanly)

        return stories
    }()
}
