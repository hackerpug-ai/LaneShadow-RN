{
  "name": "@laneshadow/server",
  "version": "0.0.0",
  "private": true,
  "description": "LaneShadow backend workspace root for Convex operations",
  "scripts": {
    "dev": "pnpm run convex:dev",
    "deploy": "pnpm run convex:deploy",
    "codegen": "pnpm run convex:codegen",
    "dashboard": "pnpm run convex:dashboard",
    "convex:dev": "sh -c 'if [ ! -d convex ]; then echo \"Missing server/convex. Establish backend code in a downstream restructure task before running Convex commands.\"; exit 1; fi; if [ \"${1:-}\" = \"--\" ]; then shift; fi; npx convex dev \"$@\"' --",
    "convex:deploy": "sh -c 'if [ ! -d convex ]; then echo \"Missing server/convex. Establish backend code in a downstream restructure task before running Convex commands.\"; exit 1; fi; if [ \"${1:-}\" = \"--\" ]; then shift; fi; npx convex deploy \"$@\"' --",
    "convex:codegen": "sh -c 'if [ ! -d convex ]; then echo \"Missing server/convex. Establish backend code in a downstream restructure task before running Convex commands.\"; exit 1; fi; if [ \"${1:-}\" = \"--\" ]; then shift; fi; npx convex codegen \"$@\"' --",
    "convex:dashboard": "sh -c 'if [ ! -d convex ]; then echo \"Missing server/convex. Establish backend code in a downstream restructure task before running Convex commands.\"; exit 1; fi; if [ \"${1:-}\" = \"--\" ]; then shift; fi; npx convex dashboard \"$@\"' --"
  },
  "dependencies": {
    "@convex-dev/geospatial": "^0.2.1",
    "@mariozechner/pi-ai": "^0.73.1",
    "convex": "^1.34.1"
  },
  "engines": {
    "node": ">=20"
  }
}
