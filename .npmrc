# Expo's native-module autolinking expects a flat node_modules. pnpm's default
# symlinked (isolated) layout can break it; `node-linker=hoisted` is the
# officially recommended Expo + pnpm setting.
# https://docs.expo.dev/guides/using-pnpm/
node-linker=hoisted
