"""Route embedding generation pipeline (INF-004).

This module provides batch embedding generation for curated routes using
OpenAI's text-embedding-3-small model (1536 dimensions).
"""

# Empty __init__.py - import directly from batch_embed_routes module
# to avoid circular import issues with the package structure.

