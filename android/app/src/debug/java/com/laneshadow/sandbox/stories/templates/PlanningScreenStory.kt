package com.laneshadow.sandbox.stories.templates

import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.ui.Modifier
import com.laneshadow.sandbox.mockproviders.PlanningMockProvider
import com.laneshadow.theme.LaneShadowTheme
import com.laneshadow.ui.templates.PlanningScreen
import com.nativesandbox.model.ComponentTier
import com.nativesandbox.model.PreviewMode
import com.nativesandbox.model.Story

/**
 * DEPRECATED: Use Sprint04PlanningStories instead.
 *
 * This file contains legacy preview stories for the NativeSandbox framework.
 * The canonical story IDs are now defined in Sprint04PlanningStories.kt
 * with proper theme variants (light and dark).
 */
object PlanningScreenStory {
    val all: List<Story> = emptyList()
}
